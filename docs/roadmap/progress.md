# D&D 2024 Remediation — Progress Log

Tracks phase completion against `docs/roadmap/dnd-2024-gap-analysis.md`. One entry per completed phase.

## Phase 1 — P0 rule bugs (done)

**P0-1: Long Rest hit-dice recovery formula.**
- `computeHitDiceRestore()` (`packages/server/src/services/rests.ts`) now takes an `edition: '2014' | '2024'` parameter. 2024 restores all spent hit dice; 2014 keeps the prior `floor(total/2)` (min 1) formula.
- `performRest()` now fetches the campaign's `srd_edition` and passes it through, following the existing edition-branching pattern already used in `services/encounters.ts` (`assessEncounterXp`, movement).
- Rule source: `docs/players-handbook-2024/Rules Glossary/rulesGlossary.md:1135`, "Long Rest" § Benefits of the Rest — "You regain all lost Hit Points and all spent Hit Point Dice."
- Open Question 2 (gap analysis) resolved by following the codebase's existing precedent: edition-branch rather than pick one formula for both editions.
- Tests: `services/rests.test.ts` (unit, both editions), `services/rests.performRest.integration.test.ts` (live DB, 2024 campaign — updated to assert full restoration, which fails against the old half-formula).

**P0-2: Concentration DC cap.**
- `computeConcentrationDc()` (`packages/server/src/services/concentration.ts`) now applies `Math.min(30, ...)`.
- Rule source: `docs/players-handbook-2024/Rules Glossary/rulesGlossary.md:515`, "Concentration" — "up to a maximum DC of 30."
- Tests: `services/concentration.test.ts` gains a boundary case (60→30, 62→30 regression, 100→30).

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB): 647/648 pass. The one failure (`campaignClasses.integration.test.ts`, "a local override in one campaign never affects the same class imported into a different campaign") is a pre-existing, unrelated test-data collision — confirmed present on `main` before this phase's changes (verified via `git stash` + re-run against a freshly re-seeded DB). Not touched this phase.
- `git status`: only files listed above modified, no scope violations.

**Not done / stopped for:** nothing — no dependency/schema/migration/deletion needed for this phase.

## Phase 2 — P1 core play loop (done)

**P1-1: Death saving throw state machine.**
- Rules grounded in `docs/rules/death-saving-throws.md` (full citation chain into `.opencode/skills/dnd5e-srd/references/2014|2024/combat.md` and `conditions.md`) before writing any code, per this project's own "consult the rules agent first" convention. The mechanic is confirmed identical between 2014 and 2024; only "knocking a creature out" differs by edition, and that mechanic is explicitly deferred (below).
- Schema: `db/migrations/1784269831666_add-death-save-columns.ts` adds `characters.death_save_successes`/`death_save_failures` (`SMALLINT 0-3`) and `characters.is_stable` (`BOOLEAN`). `is_alive` (pre-existing, previously never written after INSERT) becomes the real terminal "dead" state. "Stable" is a column, not an `active_effects` row — confirmed via the skill's own condition catalog query that it isn't one of the 15 SRD conditions in either edition.
- `services/characters.ts`:
  - `applyDamage` — inside the existing locked transaction, now computes the massive-damage/overkill formula (`overkill = damage-that-reached-hp_current − pre-hit hp_current`; `overkill >= hp_max` ⇒ instant death, independent of the failure counter), the falling-unconscious transition (applies a real `Unconscious` `active_effects` row via a new `applyOrRemoveUnconsciousEffect` helper), damage-at-0-HP failures (1, or 2 on a critical hit, reusing the same `isCritical` the damage pipeline already derives server-side), and stability breaking on any damage.
  - `applyHpDelta` (the plain heal/manual-correction path) — rejects a positive delta against an `is_alive = false` character ("can't regain hit points until magic such as revivify"), and resets the death-save counters/`is_stable`/Unconscious on any heal that brings a 0-HP character above 0 — a second, easy-to-miss code path implementing the same §1.4 reset as a nat-20 death save.
  - New `rollDeathSave(pool, actorId, characterId)` — `POST /characters/:id/death-save`, no request body, server-rolls the d20 (finally giving `dice_rolls.roll_type = 'death_save'` a real writer). Rejects (409) unless `hp_current === 0 && is_alive && !is_stable`. Implements nat-1 (2 failures), nat-20 (regain 1 HP, full state clear), 3-success stabilization (wipes both counters), 3-failure death.
  - New `stabilizeCharacter(pool, actorId, targetCharacterId, input)` — `POST /characters/:id/stabilize` (`:id` = target, `helperCharacterId` + `modifier` in the body), the Help-action DC 10 Wisdom (Medicine) check. Authorized by control of the **helper**, not the target. `modifier` is client-supplied, matching this app's existing trust model for every other skill check (`services/diceRolls.ts`'s `rollDice` never re-derives ability-score/proficiency math server-side either) — only the d20 itself is rolled server-side.
- `services/encounters.ts`'s `advanceTurn` gains a `deathSaveDue: boolean` on `AdvanceTurnResult` — true when the participant whose turn just started is a character at 0 HP, alive, and not Stable. Prompt only, matching the existing `CONCENTRATION_CHECK_PROMPTED` pattern — the roll itself stays a separate call.
- Explicit resolution of the one SRD silence flagged by the rules doc: a hit that breaks Stable status does **not** itself count as the new sequence's first failure (§1.6's silence on the interaction; documented in code comments pointing back to the rules doc).
- Tests: `services/characters.deathSaves.integration.test.ts` (new, 20 cases, live DB) — falling unconscious, massive damage at/under the exact threshold (boundary-tested via retry-on-actual-die-value, no RNG mocking, matching this codebase's existing "real RNG" testing convention), damage-at-0-HP failures (including critical doubling), massive damage overriding the failure counter, breaking Stable, all four `rollDeathSave` rejection/outcome branches (illegal states, nat 1, nat 20, ordinary success/failure), `stabilizeCharacter` success/failure/rejection, and the `applyHpDelta` heal-reset/dead-reject paths.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB): 667/668 pass (648 pre-Phase-2 + 20 new, minus the 1 pre-existing unrelated failure carried over from Phase 1's note; `campaignClasses.integration.test.ts` still not touched this phase). Ran the new file 4x in a row with no flakes.
- `npm run migrate` applied cleanly against the live dev DB; confirmed the new columns and their `CHECK` constraints via `\d characters`.

**Not done / stopped for — scoped out with the user before starting, each a separate follow-up item:**
- **`monster_instances` death-save opt-in** (`uses_death_saves`, per the rules doc's §1.9/§2.2 "mighty villain" exception) — monsters keep today's unconditional instant-death-at-0-HP default, unchanged.
- **"Knocking a creature out"** — a genuine 2014-vs-2024 mechanical difference (2014: 0 HP + Stable; 2024: 1 HP, never touches 0-HP/Stable at all) documented in `docs/rules/death-saving-throws.md` §1.8, not implemented.
- **1d4-hour natural stabilized recovery** — no time-advancement mechanism exists anywhere in this app to hang the roll off of; a Stable character currently has no path back to 1 HP without an explicit heal.
- **`undoLastDamage` does not rewind death-save state** — it still only restores `hp_current`/`hp_temp` from `last_hp_snapshot`; a DM undoing damage that killed or downed a character must currently also manually fix `is_alive`/the counters. Flagged in a code comment at the snapshot site rather than silently left undocumented.
- **Advantage/bonus-granting features on a death save** (§1.2 — a spell/feature that grants advantage or a flat bonus "on saving throws") — no such catalog data or hook exists yet; the base roll stays flat DC 10 with no modifier.

**P1-2: Auto-apply species/background mechanical grants at character creation (scoped).**
- Consulted the `dnd-rules` agent first, which also surveyed the current catalog/character-creation code before any design decision (per this project's own convention). That survey surfaced two catalog gaps not previously itemized in `dnd-2024-gap-analysis.md`: **tool proficiencies have no schema anywhere** (no catalog table, no character join table), and **background starting gear/gold is dropped at seed time**, not merely unwired. It also found only **4 of the 16 official 2024 backgrounds are seeded at all** (Acolyte, Criminal, Sage, Soldier).
- Given that, and the gap analysis's own Open Question 3 (whether full automation is even the intended design direction), scope was confirmed with the user before writing any code. Chosen scope: pre-fill only what the catalog can fully answer without an unmodeled player choice or missing schema, and never reject a client-supplied value that contradicts the catalog (a client can always override).
- **In scope, implemented:**
  - Ability-score bonus — **2014 race/subrace only** (fixed, no player choice: e.g. Dwarf CON+2, Hill Dwarf +1 WIS, confirmed against the live seed). Always added on top of the client's submitted base score when a `raceId`/`subraceId` is set — not offered as a skippable default, since there's no legitimate "apply this race but not its bonus" case.
  - Speed — pre-filled from `races.speed` when the client omits it. Required dropping `speed`'s create-time schema default (`schemas/characters.ts`) from `.default(30)` to `.optional()` so "the client sent nothing" becomes observable to the service instead of always arriving as 30; the wizard already never sends `speed` today, so this is a pure behavior improvement with no frontend change needed.
  - Darkvision → `senses` — parsed from the race's `traits` JSONB (`services/characters.ts`'s new `darkvisionFeetFromTraits`). The seed encodes this two different ways per edition, confirmed against live data rather than assumed: 2014 uses a bare `'darkvision'` index (that edition's one uniform 60 ft radius); 2024 suffixes the actual feet value (`'darkvision-60'`, `'darkvision-120'` for Dwarf/Orc's non-standard range). Only fills `senses` when the client didn't already send one.
  - Background skill proficiencies and granted feat — auto-inserted into `character_skill_proficiencies`/`character_feats` in the same transaction as character creation, from `backgrounds.skill_proficiency_ids`/`granted_feat_id`. `ON CONFLICT DO NOTHING` on both, an explicit **app-design choice, not an SRD rule** — the agent's rules lookup confirmed the SRD corpus is silent on what happens when a background-granted skill overlaps one a class also grants.
- **Deliberately out of scope this pass (each a real, separate follow-up, not silently dropped):**
  - **2024 background ability-score bonus** — genuinely requires a player CHOICE (+2/+1 vs. +1/+1/+1, and which of the background's 3 listed abilities gets which) that no part of this app collects today (the wizard's `AbilityScoresStep` has zero `backgroundId` linkage). Guessing a distribution would be worse than not automating it; needs a wizard UI change first.
  - **Tool proficiencies** — no schema at any level (catalog or character-side); a background's granted tool (e.g. Acolyte's Calligrapher's Supplies) currently can't be persisted at all.
  - **Starting gear/gold** — background equipment/gold data is dropped at seed time, before it ever reaches a `backgrounds` column; needs its own seed + schema change.
  - **Race-granted damage resistances** — several are themselves a player choice (Dragonborn's draconic-ancestry ties its resistance type; Tiefling's Fiendish Legacy is a chosen legacy), not worth doing only for the fixed-resistance minority (Dwarf's poison resistance, 2014 Tiefling's fixed fire resistance).
  - **Catalog completeness** (FT-01/SB-01/SB-02, and the newly-found 4/16-backgrounds gap) — unchanged this phase; the automation above is real but will only ever fire for whatever fraction of species/backgrounds are actually seeded.
- Tests: `services/characters.speciesBackgroundGrants.integration.test.ts` (new, 10 cases, live DB) — race+subrace bonus stacking, speed/senses pre-fill vs. explicit-override, the no-`raceId` no-regression case, background skill+feat auto-grant, the explicit "2024 gets no ability bonus yet" regression guard, and the no-`backgroundId` no-op case. Catalog row ids are looked up by `index_key` in the test setup, not hardcoded, so this survives a reseed.

**Verification:**
- `npx tsc -b --force` in both `packages/server` and `packages/web`: clean (the `speed` schema type change is shared, so both were checked).
- `npm test` in `packages/server` (live DB): 677/678 pass (668 prior + 10 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision noted in Phase 1/P1-1, still untouched).
- No new migration — everything reads from and writes to columns/tables that already existed.

**P1-3: Complete species catalog (Aasimar; Dragonborn trait depth).**
- Before touching seed code, compared the third-party dataset's actual content against this repo's own `docs/players-handbook-2024/Chapter 4- Character Origins/chapter4-characterOrigins.md` (lines 465-860 — the full official 2024 species section) species-by-species, rather than trusting the gap analysis's "L effort" estimate at face value. Result: **only Dragonborn is actually sparse** — every other already-seeded 2024 species (Dwarf, Elf, Gnome, Goliath, Halfling, Human, Orc, Tiefling) already lists 100% of its official named traits. This narrows P1-3's real remaining scope well below the roadmap's original estimate.
- **Provenance decision, made explicit rather than silently resolved:** Aasimar is entirely absent from the third-party SRD JSON dataset (`.opencode/skills/dnd5e-srd/data/2024/5e-SRD-Species.json` — confirmed by direct inspection, 9 entries, no `aasimar` key) in both editions' underlying race files, so it cannot be "found" in that corpus at all. Fixed by hand-authoring Aasimar's catalog row directly from the PHB text above, in `packages/server/src/db/seeds/catalog.ts` (`AASIMAR_2024_SPECIES` constant + a small dedicated insert block, reusing the exact same upsert statement as the JSON-driven loop so a reseed stays idempotent) — **not** by editing the third-party JSON file itself, which would blur that file's value as a verifiable copy of the upstream dataset. This is a narrow, explicitly-scoped exception to "seed data comes from the SRD JSON," not a resolution of Open Question 1 (broader catalog provenance) — that question remains open for the class/subclass/feat catalogs P1-4/P1-5 will touch.
- Dragonborn's 3 missing traits (Draconic Ancestry, Breath Weapon, Damage Resistance — confirmed present in 2014's version of the same dataset with these exact same 3 generic names, so the 2024 JSON file's omission looks like an upstream authoring gap, not an intentional difference) are merged in at seed time via `DRAGONBORN_2024_TRAIT_SUPPLEMENT`, appended to whatever the JSON row already provides rather than replacing it.
- 2014 species/races are unaffected and intentionally not touched — Aasimar isn't a core 2014 PHB race either (a later supplement), matching this project's "2014 isn't independently re-verified" convention.
- Ran `npm run seed` against the live dev DB: `races` count went from 18 to 19 (9×2014 + 10×2024). Confirmed via direct SQL: Aasimar row present with all 5 traits and `size = 'Medium or Small'`; Dragonborn 2024 row now has all 5 traits.
- Tests: `services/catalog.speciesCompleteness.integration.test.ts` (new, 4 cases, live seeded DB, no fixtures) — all 10 2024 species present including Aasimar, Aasimar's exact trait set and size, Dragonborn's full 5-trait set, and a regression guard that 2014 still has no Aasimar row.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 681/682 pass (677 prior + 4 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision, still untouched).
- No migration needed — `races.traits`/`ability_bonuses` were already `JSONB`; this phase only changed seed data.

**Not done / stopped for:**
- **Feat catalog completeness (FT-01)** and **subclass catalog completeness (P1-5)** — unchanged; both still depend on Open Question 1's broader provenance decision, which this phase deliberately did not resolve (it only carved out one narrow, explicit exception for species data).
- **2014 species/race trait depth** — not audited this phase; scope was 2024 only, matching SB-01/SB-02's own framing and the acceptance criteria's "10/10 species" (the 2024 species list specifically).

**P1-4: Complete feat catalog (Origin/General/Fighting Style/Epic Boon, all official 2024 feats).**
- **Corrected a stale count before doing any work**, rather than building against it: FT-01 and this roadmap's own executive summary state "69 total (10 Origin + 37 General + 10 Fighting Style + 12 Epic Boon)." Direct inspection of this repo's own `docs/players-handbook-2024/Chapter 5- Feats/chapter5- feats.md` (all 1215 lines read in full) shows the actual official count is **75** — General is 43, not 37; everything else matches. The roadmap's "37" appears to have been a stale/incorrect estimate, not a citation error on this pass's part. All seed data and the test below are built against the verified 75, and this correction is recorded here so future roadmap references don't keep propagating "69."
- Confirmed scope with the user before writing code, given two real decisions: (a) `feats` had no `type`/category column at all — needed to actually check the acceptance criteria's per-category counts — so a migration was required; (b) the real remaining work was ~58 missing feats, not the ~52 the stale total implied. User chose: add the column, and do the full 58-feat pass in one session rather than splitting it.
- Schema: `db/migrations/1784269832666_add-feats-type.ts` adds `feats.type` (`TEXT CHECK IN ('origin','general','fighting_style','epic_boon')`), nullable — 2014's minimal feat set (this seed carries only Grappler) predates this 2024-only categorization and is deliberately left unclassified rather than force-fit into a bucket.
- **Same provenance approach as P1-3**: the third-party SRD JSON dataset carries only 17 of the 75 feats and has no type/category field on any of them. All 58 missing feats, plus a `type` backfill for the 17 that DO come from the JSON, are hand-authored directly in `db/seeds/catalog.ts` (`SUPPLEMENTAL_2024_FEATS` + `FEAT_TYPE_BY_INDEX`) from the PHB chapter above — not written into the third-party JSON file itself, for the same "don't blur that file's upstream provenance" reason as Aasimar.
- **Caught and fixed a real bug before it shipped**: the first pass applied `FEAT_TYPE_BY_INDEX` by index alone, with no edition guard — since 2014 and 2024 both seed a `grappler` row, this silently mislabeled 2014's Grappler as `type = 'general'` (a 2024-only category that shouldn't apply to it at all). Caught by inspecting the seeded output directly (not by the test suite, which didn't check this at the time) and fixed by gating the backfill to `edition === '2024'`; a regression test for exactly this case was added afterward.
- Distances in the 58 new descriptions are written in meters, matching this catalog's own established convention (every other seeded description — spells, conditions, race traits — already uses meters per the SRD JSON's own feet-to-meters authoring), not the feet used in the PHB source text — converted by hand per value (5 ft = 1.5 m, 10 ft = 3 m, 30 ft = 9 m, 60 ft = 18 m), so a feat's hand-authored provenance is invisible to anyone just reading the catalog.
- Ran `npm run seed` against the live dev DB: `feats` count went from 18 to 76 (75 official 2024 + 2014's Grappler). Confirmed via direct SQL: category breakdown is exactly origin=10, general=43, fighting_style=10, epic_boon=12; 2014's Grappler has `type = NULL`.
- Tests: `services/catalog.featCompleteness.integration.test.ts` (new, 5 cases, live seeded DB, no fixtures) — exact total (75), exact category breakdown, no unclassified 2024 rows, the 2014-Grappler-stays-unclassified regression guard (the bug caught above), and that every one of the 4 seeded 2024 backgrounds' `granted_feat_id` still resolves to a real feat row.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 686/687 pass (681 prior + 5 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision, still untouched).

**Not done / stopped for:**
- **2014 feat depth** — out of scope; 2014's feat system is a PHB variant rule, not core SRD content, and this project doesn't independently re-verify 2014 rules text (per the gap analysis's own "Not doing / out of scope" section).
- **Feat prerequisites are display-only text**, not mechanically enforced (e.g. nothing stops a level-1 character from taking a "Level 4+" feat) — matches this app's existing, already-established convention for `saveDc` and similar manually-entered fields (`docs/rules/death-saving-throws.md`'s own citation of this precedent); not a new gap introduced by this phase.

**P1-5: Complete subclass catalog (4 per class, for the classes this project can actually verify).**
- Directly confronted Open Question 1 (catalog provenance) rather than routing around it, since P1-5 explicitly names it as a dependency. Investigated first, before writing anything: (a) the third-party SRD JSON dataset has **exactly 1 subclass per class for all 12 classes** — confirmed by direct inspection of `5e-SRD-Subclasses.json`, not a seeding gap, the source data itself stops at 1; (b) this repo's own PHB docs (`docs/players-handbook-2024/Chapter 3- Character Classes/chapter3-characterClasses.md`, read end-to-end, all 3300 lines) cover only 6 of 12 classes — Barbarian, Bard, Cleric, Druid, Fighter, Monk. Paladin, Ranger, Rogue, Sorcerer, Warlock, and Wizard have **zero source text anywhere in this project**, not in the PHB docs and not beyond 1 subclass in the third-party dataset.
- Confirmed scope with the user before writing any content, given both the provenance wall and the sheer size (completing even just the 6 verifiable classes meant authoring 18 new subclasses with full tiered mechanical features — a larger transcription job than P1-4's 58 feats). User chose: leave the 6 unverifiable classes at their existing 1 subclass each rather than complete them from uncited knowledge, and do all 6 verifiable classes' remaining subclasses in one pass rather than splitting further.
- All 18 missing subclasses (Barbarian: Path of the Wild Heart, Path of the World Tree, Path of the Zealot; Bard: College of Dance, College of Glamour, College of Valor; Cleric: Light Domain, Trickery Domain, War Domain; Druid: Circle of the Moon, Circle of the Sea, Circle of the Stars; Fighter: Battle Master, Eldritch Knight, Psi Warrior; Monk: Warrior of Mercy, Warrior of Shadow, Warrior of the Elements) hand-authored from the PHB chapter above, each with its full tiered feature set (levels 3 through 17/18 as applicable) — not summarized or abbreviated. Battle Master's 20-maneuver "Maneuver Options" list is captured as one consolidated feature row rather than 20 separate rows, matching this table's existing "a feature is a named block of rules text" shape. Same meters-conversion and paragraph-format conventions as P1-3/P1-4.
- Schema/wiring: `db/seeds/catalog.ts` gained `SUPPLEMENTAL_2024_SUBCLASSES` (class index, subclass index/name, and a `features: [{level, name, description}]` array per subclass), wired into **both** `seedClassesAndSubclasses` (the `subclasses` table) and `seedClassFeatures` (the `class_features` table) — the latter matters specifically because `seedClassFeatures` unconditionally `DELETE`s and fully repopulates `class_features` on every seed run (pre-existing idempotency design, not new), so the supplemental features had to be added inside that same function or they'd vanish on the next reseed. No migration needed — `subclasses`/`class_features` already had every column this required.
- Each of the 6 classes' pre-existing third-party-JSON subclass (Path of the Berserker, College of Lore, Life Domain, Circle of the Land, Champion, Warrior of the Open Hand) is one of the PHB's 4 canonical options for that class and was left untouched — only the 3 missing canonical ones per class were added.
- Ran `npm run seed` against the live dev DB: `subclasses` went from 12 to 42 (12×2014 + 12×2024-JSON-sourced + 18 new), `class_features` went to 728. Confirmed via direct SQL: the 6 verified classes each show exactly 4 subclasses with `class_features` rows; the 6 unverified classes remain at exactly 1 each, untouched.
- Tests: `services/catalog.subclassCompleteness.integration.test.ts` (new, 4 cases, live seeded DB, no fixtures) — the 6 verified classes each have exactly 4 subclasses, the 6 unverified classes remain at exactly 1 (a regression guard against ever silently "completing" them without a source), every supplemental subclass has at least one wired `class_features` row, and Battle Master's maneuver list and tiered features all survive.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 690/691 pass (686 prior + 4 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision, still untouched).

**Not done / stopped for:**
- **The other 6 classes' subclass catalogs (Paladin/Ranger/Rogue/Sorcerer/Warlock/Wizard)** — deliberately left at 1 subclass each. Completing them requires either a new authoritative source entering this project (e.g. adding their PHB chapter text under `docs/players-handbook-2024/`) or an explicit decision to accept unverified content; neither happened this phase.
- **Open Question 1 itself** — still open. This phase resolved the narrower, practical question ("can P1-5 make any progress at all") without resolving the project's general catalog-provenance policy, exactly as P1-3/P1-4 did for species and feats.
- **Class-level (non-subclass) feature depth for the 6 verified classes** (e.g. core Barbarian/Bard/etc. features outside their subclasses) — not audited this phase; P1-5's acceptance criteria is specifically about subclass completeness, and the base class features weren't in scope.

**P1-6: Implement Weapon Mastery mechanical effects.**
- Consulted a research pass first (survey of catalog/schema, attack/damage pipeline, `active_effects` as a reuse candidate, and the exact PHB mechanical text for all 8 properties) before any design decision, per this project's convention. That surfaced real structural gaps beyond "wire up 8 properties": `character_attacks` rows had no link to any catalog weapon at all, there is no single server-side hit/miss adjudication function, and Vex/Sap's "advantage/disadvantage on a future roll" would require `rollDice` to consult live effect state, which it never does today — the same unimplemented gap as CB-07/P2-2.
- **Confirmed scope with the user before writing code** (gap analysis Open Question 3, "mechanical automation vs. manual DM adjudication," explicitly names P1-6 as blocked on this). Three decisions:
  1. **Automation depth: track-state only, not full automation.** Sap/Vex/Slow write a real `active_effects` row (visible, DM/player-adjudicated) but nothing rewires `rollDice` to auto-consult it — matches this codebase's own established "track state, let the DM adjudicate" philosophy (`movement.ts`/`rests.ts` comments). CB-07/P2-2 remains the ticket for closing that loop generally, not reopened here.
  2. **`character_attacks.item_id`** (proper FK to `items`, not a narrower mastery-only column) — reusable beyond this feature, and the only way the server can ever know which catalog weapon (and therefore which mastery property) an attack row represents.
  3. **Known-masteries class scope: all 12 classes, not just the 2 this project's PHB docs cover.** Only 5 of 12 classes ever get Weapon Mastery at all (Barbarian, Fighter, Paladin, Ranger, Rogue — confirmed via live web search, 2026-08-30, cross-checking dndbeyond.com/posts/1742 and independent secondary sources against training-data recollection); this project's own `docs/players-handbook-2024/` only has source text for Barbarian and Fighter. The user chose to include Paladin/Ranger/Rogue's counts anyway rather than leave them unautomatable, explicitly accepting that those 3 classes' numbers are **not verified against this project's own PHB doc set** the way Barbarian/Fighter's are (flagged prominently in `db/seeds/catalog.ts`'s `WEAPON_MASTERY_THRESHOLDS` comment) — a deliberate, scoped exception to this project's usual provenance discipline (P1-3/P1-4/P1-5's "don't fabricate what isn't sourced here"), not a quiet erosion of it.
- **Also fixed a real data bug found during the research pass**: the seed's `shortbow` mastery was `slow`; the verified weapon->mastery table (`.claude/skills/dnd-2024-rules/references/equipment-and-weapon-mastery.md`, itself sourced from this project's own PHB Ch6) says `vex`. Corrected in `db/seeds/catalog.ts`.
- Schema: `db/migrations/1784269833666_add-weapon-mastery-tracking.ts` — `character_attacks.item_id` (nullable UUID FK to `items`), `class_levels.weapon_mastery_count` (nullable SMALLINT — NULL for the 7 classes that never get the feature, which is the mechanically correct value, not a gap), and a new `character_weapon_mastery_choices` table (character_id, item_id, unique pair) for the actual player choice ("which weapon kinds have you drilled").
- Seed: `seedWeaponMasteryCounts` (`db/seeds/catalog.ts`) populates `weapon_mastery_count` for all 20 levels of the 5 relevant classes from a threshold table (Barbarian 2→3@4→4@10; Fighter 3→4@4→5@10→6@16; Paladin/Ranger/Rogue static 2). 3 new `effect_definitions` rows (`Sap (Weapon Mastery)`, `Vex (Weapon Mastery)`, `Slowed (Weapon Mastery)`) alongside the existing Dodge/Hidden non-condition templates; Topple deliberately gets no new template since its result IS the existing Prone condition, applied through the effects endpoints that already exist.
- `services/weaponMastery.ts` (new):
  - `listCharacterWeaponMasteries`/`setCharacterWeaponMasteries` — the known-masteries choice set, capped by `SUM(class_levels.weapon_mastery_count)` across the character's classes at their current level in each. The sum-across-classes rule for multiclass characters is an explicit, documented inference (the PHB's multiclassing table doesn't list Weapon Mastery at all) — flagged in code as unverified, same treatment as P1-1's death-save silence resolution.
  - `resolveWeaponMasteryTrigger` — `POST /characters/:id/weapon-mastery-trigger` (`:id` = the attacking character). Resolves the weapon's mastery property (via `weaponItemId` or `characterAttackId`), rejects if the attacker hasn't chosen that mastery, then branches: Sap (target, no damage required)/Vex (attacker, requires damage dealt)/Slow (target, requires damage dealt, won't stack a second row while one is active — RAW's "doesn't exceed 10 feet") write a real effect via `effects.ts`'s `insertActiveEffect` (newly exported for this — player-authorized, not the DM-only `applyCharacterEffect` path, since this narrates the attacker's own already-resolved action); Cleave/Graze/Nick/Push/Topple return the exact PHB mechanical text and `applied: false`, resolved by the caller through endpoints that already fully exist (a second damage-application call, a token move, the normal effects endpoint for Topple's Prone).
  - Authorization: control of the attacking character (`authorizeCharacterAction`, same as `applyDamage`'s own convention), not DM-only — this narrates a player's own attack, it doesn't let anyone apply an arbitrary effect to anyone.
- `schemas/damage.ts`'s `applyDamageSchema.diceCount` relaxed from `.min(1)` to `.min(0)` — lets Graze's flat ability-modifier damage-on-a-miss reuse the existing `applyDamage` endpoint directly (`computeAppliedDamage` already handles an empty rolled-dice array correctly) instead of needing a second damage-application function.
- `character_attacks` create/update now accept an optional `itemId`, validated to reference a real weapon item (`services/characterAttacks.ts`'s `assertItemIsWeapon`).
- Tests: `services/catalog.weaponMasteryCompleteness.integration.test.ts` (new, 8 cases, live seeded DB, no fixtures) — all 8 properties present, the shortbow regression guard, every seeded weapon has a valid mastery key, Barbarian/Fighter's exact threshold table, Paladin/Ranger/Rogue's static-2 guard, the NULL-for-non-mastery-classes guard, the 2014-has-none guard, and the 3 effect templates seeded. `services/weaponMastery.integration.test.ts` (new, 10 cases, live DB, throwaway fixtures) — allowance computation (unclassed=0, Fighter@1=3), non-weapon rejection, over-allowance rejection, replace-not-merge on a second `setCharacterWeaponMasteries` call, rejecting a trigger for an unchosen mastery, Sap landing on the target with no damage requirement, Sap not firing on a miss, Vex landing on the attacker and requiring damage &gt; 0, and a narrative-only property (Nick) never writing an effect.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB; `npm run seed` re-run cleanly, confirmed via direct SQL: `class_levels.weapon_mastery_count` matches the threshold table exactly at levels 1/3/4/9/10/15/16/20 for all 5 classes, `shortbow` now reads `vex`, and all 3 new `effect_definitions` rows exist with the expected `duration_type`/`stacking_rule`.
- `npm test` in `packages/server` (live DB, freshly reseeded): 708/709 pass (690 prior + 18 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision noted since Phase 1, still untouched).

**Not done / stopped for — each a real, separate follow-up, not silently dropped:**
- **Full automation of Vex/Sap into the roll pipeline** (auto-deriving advantage/disadvantage in `rollDice` from live `active_effects`) — explicitly scoped out with the user; this is the same gap as CB-07/P2-2 and stays a single follow-up item rather than being half-closed here.
- **Cleave/Nick's "once per turn" limit** — not enforced server-side (no turn-scoped action-economy resource exists for this); same "no auto-trigger" precedent already accepted for CB-08 (opportunity attacks).
- **The Weapon Master feat's extra mastery slot** — `setCharacterWeaponMasteries`' allowance is computed from `class_levels.weapon_mastery_count` only; a character who took the Weapon Master feat doesn't get its +1 counted. `character_feats` would need to be joined in too — deferred rather than silently included as an unverified assumption about how the feat and class features combine.
- **Paladin/Ranger/Rogue's weapon-mastery counts are not sourced from this project's own `docs/players-handbook-2024/`** — see the scope-decision note above. Re-verify against real PHB text if/when those classes' chapters ever enter this project's doc set.
- **Weapon proficiency is not checked** before allowing a mastery choice (the PHB gates mastery on "provided you have proficiency with it") — this app has no per-character weapon-proficiency data model at any granularity finer than saving-throw/skill proficiencies, so this couldn't be enforced without its own schema addition. Matches the already-accepted "feat prerequisites are display-only text" precedent (P1-4).

**P1-7: Enforce prepared-spell cap.**
- Acceptance criteria (SP-03): `toggleCharacterSpellPrepared` rejects preparing a spell beyond `class level + spellcasting ability modifier (min 1)`.
- Research first, per this project's convention: `.claude/skills/dnd-2024-rules/references/spellcasting.md`'s "Spell Preparation by Class" table confirms the 2024 PHB unified **all 8** spellcasting classes (Bard, Cleric, Druid, Paladin, Ranger, Sorcerer, Warlock, Wizard) onto one "prepare a list of level 1+ spells" mechanic — they differ only in swap timing/quantity (e.g. Bard/Sorcerer/Warlock: 1 spell on level-up; Cleric/Druid/Wizard: any number on Long Rest), not in whether the level+mod cap formula applies. This matters because the pre-2024 mental model (Bard/Sorcerer/Warlock have a fixed "spells known" table, unrelated to ability score) is wrong for this ruleset — confirmed against this project's own doc set, not assumed from general recollection.
- **Found the missing piece the formula needs**: `classes.primary_ability_id` already exists but is the wrong ability for this — it's the *multiclass prerequisite* ability (e.g. Paladin: STR-or-CHA), not necessarily the *spellcasting* ability (Paladin's spellcasting ability is CHA alone). No column captured "which ability does this class prepare with" anywhere in the schema.
- Schema: `db/migrations/1784269834666_add-class-spellcasting-ability.ts` — new nullable `classes.spellcasting_ability_id` (UUID FK to `ability_scores`), seeded in `db/seeds/catalog.ts`'s `seedClassesAndSubclasses` straight from each class's own `5e-SRD-Classes.json`'s `spellcasting.spellcasting_ability.index` field (both editions carry this — verified identical bard=CHA/cleric=WIS/druid=WIS/paladin=CHA/ranger=WIS/sorcerer=CHA/warlock=CHA/wizard=INT across 2014 and 2024). NULL for the 4 non-caster classes (Barbarian/Fighter/Monk/Rogue) and for homebrew classes, which have no field to set it yet (see "Not done" below).
- `services/characterSpells.ts`'s new `assertWithinPreparedSpellCap`: computes `cap = max(1, character_classes.level + abilityModifier(character's own score for the class's spellcasting ability))`, then counts the character's other currently-prepared, non-cantrip (`spells.level >= 1`), non-`always_prepared` spells for that same granting class and rejects if adding one more would exceed it. Cantrips and `always_prepared` rows (granted free by another feature, per the "Always-Prepared Spells" rule in `spellcasting.md`) are exempt by design, not by omission. Wired into **both** `toggleCharacterSpellPrepared` (the acceptance criteria's explicit target) and `learnCharacterSpell` (which also accepts `isPrepared: true` directly on insert) — leaving the second uncovered would have made the first trivially bypassable via unlearn+relearn.
- When the cap can't be computed at all — no `character_classes` row for that class, or the class's `spellcasting_ability_id` is NULL (non-caster class, or a homebrew class) — the check is skipped rather than blocking, the same "can't reason about it, don't block" precedent `services/casting.ts`'s participant-less no-op branch already established.
- Tests: `services/characterSpells.preparedCap.integration.test.ts` (new, 10 cases, live DB, throwaway fixtures) — a level-1 Wizard (INT 16, mod +3, cap 4) fills its cap exactly and a 5th prepared spell is rejected; learning the same spell unprepared succeeds; toggling it to prepared still fails at the cap; un-preparing one spell frees a slot; re-toggling an already-prepared spell doesn't double-count itself against its own cap; cantrips and `always_prepared` spells are exempt even beyond the cap; a low-INT (8, mod -1) Wizard gets a floor of 1, not 0 or negative; a spell recorded against a non-caster class is never capped.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB; `npm run seed` re-run cleanly, confirmed via direct SQL: all 8 spellcasting classes (2024) carry the correct `spellcasting_ability_id`, all 4 non-casters are NULL.
- `npm test` in `packages/server` (live DB, freshly reseeded): 718/719 pass (708 prior + 10 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision noted since Phase 1, still untouched).

**Not done / stopped for:**
- **Swap timing/quantity restrictions are not enforced** (e.g. Bard can only swap 1 spell on level-up, Wizard can swap any number on Long Rest) — `toggleCharacterSpellPrepared` only enforces the total-count cap, matching this project's "track state, let the DM adjudicate" default for anything requiring turn/rest-scoped bookkeeping this app doesn't otherwise track (same category as P1-6's Cleave/Nick once-per-turn gap).
- **Homebrew classes have no way to set `spellcasting_ability_id`** — `schemas/catalogHomebrew.ts`'s `classHomebrewShape` wasn't extended (it also isn't currently wired to any class-creation route at all — only reused for `campaignClasses.ts`'s per-campaign override shape). A homebrew spellcasting class is left uncapped entirely rather than mis-capped, per the "can't reason about it, don't block" choice above; add the field if/when homebrew class creation actually ships.
- **Half-caster (Paladin/Ranger) prepared counts use the class's own unhalved level**, per the formula each class's own Spellcasting feature states — this is the correct 2024 rule (a genuine change from 2014's separate fixed "spells known" tables for these two), not an oversight, but is called out since it's easy to mistake for a halving bug given Paladin/Ranger halve their *spell slot* progression elsewhere in this codebase.

**P1-8: Implement Short Rest hit-die spending.**
- Acceptance criteria: a player-facing endpoint lets a character spend N hit dice one at a time during a short rest, rolling die + CON mod (min 1 HP each), decrementing `hit_dice_remaining`.
- `services/rests.ts`'s own header comment already named this exact gap: its bulk DM `performRest` deliberately never touches HP or `hit_dice_remaining` on a short rest because spending hit dice is "a player choice/action, not a bulk DM effect" — out of scope for that endpoint by design, not an oversight. This phase builds that missing player-facing action as its own endpoint, reusing `rests.ts`'s existing die-type progression logic rather than duplicating it (`fetchHitDieProgression` exported for this).
- `services/characters.ts`'s new `spendHitDice`, modeled directly on the existing `applyHpDelta` right above it in the same file: same `authorizeCharacterAction` control-gate ("acting right now", not sheet-editing), same `FOR UPDATE` row lock, same `hp_max` clamp via `applyHpDeltaWithTempAbsorption`'s math, same death-save-counters-reset-on-regaining-HP-from-0 logic, same per-encounter `sync_seq` bump feeding the same `broadcastHpChanged` socket event `PATCH .../hp` already uses.
- What's actually new: each die spent is **rolled independently** server-side (`rollDie` — the same "RNG lives here and only here" primitive every other roll in this app uses) and heals `roll + CON modifier`, minimum 1 HP, **per die** — spending 2 dice in one call produces 2 independent rolls/heals, not one combined roll with one floor. `dieType`/`count` are validated against the character's real hit-die progression (`fetchHitDieProgression`) and current `hit_dice_remaining[dieType]` — spending more than remains, or a die type the character doesn't have, is rejected before any roll happens.
- Route: `POST /characters/:id/spend-hit-dice` (`schemas/characters.ts`'s `spendHitDiceSchema`: `dieType` regex-shaped like `"d10"`, `count` 1-20), alongside the existing `PATCH .../hp` and `POST .../apply-damage` HP-mutating routes.
- A pre-existing, adjacent semantic gap surfaced (not fixed) during this work: `hit_dice_remaining` is `NULL`-by-default on character creation with no auto-init from `character_classes` anywhere in this codebase, and the existing long-rest restore logic (`computeHitDiceRestore`) already reads a missing die-type key as **0 remaining** (fully spent), not a full pool. `spendHitDice` deliberately reads it the same way for consistency — introducing a different interpretation for the same column in a second code path would be worse than the existing gap. Practical effect: a freshly created character with no `hit_dice_remaining` set has nothing to spend until either the character sheet explicitly initializes it or a long rest runs once. Not a new gap this phase introduced, and not silently special-cased around.
- Tests: `services/characters.spendHitDice.integration.test.ts` (new, 5 cases, live DB, throwaway fixtures) — a single die rolls d10+CON mod (min 1), heals, decrements `hit_dice_remaining`, and clamps at `hp_max`; spending 2 dice in one call produces 2 independent rolls/heals whose sum is the total; spending more than remains is rejected; a die type the character doesn't have is rejected; spending on a dead (`is_alive = false`) character is rejected.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 723/724 pass (718 prior + 5 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision noted since Phase 1, still untouched).

**Not done / stopped for:**
- **No 0-HP/unconscious gate beyond `is_alive`** — a stabilized character at 0 HP can still choose to spend hit dice through this endpoint, matching `applyHpDelta`'s existing precedent (which also only gates on `is_alive`, not `hp_current > 0`) rather than inventing a stricter rule nothing else in this codebase enforces.
- **The `hit_dice_remaining`-defaults-to-NULL gap** described above is unfixed — flagged as a real, separate follow-up rather than papered over with a second, inconsistent interpretation.
- **This endpoint doesn't require an actual short rest to be in progress** (no `rest_events` row, no campaign-level "are we currently resting" state) — 5e lets a player spend hit dice during a short rest specifically, but this app has no session-scoped "short rest in progress" state machine to gate on, and `rests.ts`'s own `performRest` is a discrete bulk action, not an ongoing state. Same "track state, let the DM adjudicate" treatment as P1-6/P1-7's swap-timing gaps.

**P1-9: Implement Surprise.**
- Acceptance criteria (roadmap text, as written): "an encounter can mark specific participants Surprised at combat start per the source rule; Surprised participants can't move/act/react on their first turn."
- **Research surfaced a real rules contradiction before any code was written.** This project's own verified 2024 rules reference (`.claude/skills/dnd-2024-rules/references/combat.md` and `2014-vs-2024-differences.md`, both sourced from `docs/players-handbook-2024/`) states the 2024 PHB's *sole* mechanical effect of Surprise is Disadvantage on the surprised creature's Initiative roll, and explicitly flags "surprised = lose your turn" as a common misreading carried over from prior editions that does **not** apply in 2024. The roadmap's "can't move/act/react on their first turn" text is, verbatim, the 2014 SRD rule (`.opencode/skills/dnd5e-srd/references/2014/combat.md` line 19) — accurate for 2014, contradicted by this project's own 2024 source for 2024. Since this project runs both editions per-campaign, this couldn't be resolved by picking one literal reading; **confirmed with the user via AskUserQuestion** before implementing: edition-aware behavior (2014 gets the full lockout; 2024 gets Initiative-Disadvantage-only), matching P0-1's precedent of edition-branching a rule the two rulesets genuinely disagree on, over either (a) implementing the literal 2014-style lockout for both editions (knowingly wrong for 2024) or (b) dropping the lockout mechanic entirely for both.
- Schema: `db/migrations/1784269835666_add-surprise-tracking.ts` — one column, `combat_participants.is_surprised BOOLEAN NOT NULL DEFAULT false`. No new `effect_definitions`/conditions catalog row: Surprise isn't in either edition's formal Conditions list (it's combat-start-only text in the same PHB chapter, not a mechanic the Prone/Restrained-style condition system covers), so a plain column fits its actual scope better than routing it through `active_effects`.
- Marking surprise: bundled into the existing atomic `startCombat` action (`POST /encounters/:id/start-combat`, now accepting an optional `{ surprisedParticipantIds: string[] }` body — `schemas/encounters.ts`'s `startCombatSchema`) rather than a separate pre-step endpoint. This project doesn't automate surprise *detection* (Stealth vs. passive Perception is a DM call, made with the existing dice-roll tools before clicking "Start combat"), and 2024's Disadvantage effect has to land on the SAME Initiative roll `startCombat` already performs — a separate "mark surprised" call before it would need its own transaction coordination for no real benefit. `is_surprised` is reset for every participant (not just the named ones) on each `startCombat` call, mirroring `current_round`'s existing "fresh state each time combat (re)starts" rule.
- 2024's effect: `rollAndReorderInitiative` (shared by `startCombat`/`rollInitiative`/mid-combat joins) now takes the campaign's `srdEdition` and, for a `2024` + `is_surprised` participant, rolls 2d20 and keeps the lower before adding the Dex modifier — the same "RNG lives here and only here" primitive every other roll in this app uses, just called twice.
- 2014's effect: two new small gates, `services/encounters.ts`'s `requireNotSurprised` (pure, alongside the existing `requireCurrentTurn`) wired into both action-economy spends (`applyActionEconomy` — action/bonus action/object interaction/movement rejected on the surprised participant's own turn, reaction rejected regardless of whose turn it is) and token movement (`computeValidatedMoveCost`, the same DM-exempt gate `requireCurrentTurn` already uses there). `is_surprised` is cleared automatically the moment the surprised participant's own first turn ends (`advanceTurn`, using the encounter's pre-advance `current_turn_index` to identify whose turn just finished) — since it's never set again after that, "`is_surprised` still true" doubles as "this participant's first turn hasn't fully elapsed yet" with no separate first-turn marker needed, matching the 2014 rule's own two-part timing (locked on their own first turn; locked out of reactions from combat start until that turn ends, regardless of whose turn it currently is).
- Tests: `services/encounters.surprise.integration.test.ts` (new, 7 cases, live DB, throwaway fixtures, one 2024-edition campaign and one 2014-edition campaign) — `startCombat` sets `is_surprised` only for the named participants and resets everyone else; a 2024 surprised participant's Initiative roll makes 2 calls to the RNG (disadvantage) vs. 1 for a normal roll (`vi.spyOn(Math.random)`, call-through, asserting call count rather than a specific roll — deterministic without needing to control/predict RNG output); a 2024 surprised participant can still act/move/react freely (no lockout); a 2014 surprised participant's Initiative roll makes only 1 RNG call (no Disadvantage in 2014); a 2014 surprised participant's own action/movement/reaction are all rejected with `reason: 'SURPRISED'` while an un-surprised co-participant's rejection is still plain `NOT_YOUR_TURN`; the DM can still move a surprised participant's token (existing DM-override precedent, unaffected); `is_surprised` clears once the surprised participant's first turn ends via `advanceTurn`, and a reaction spend that failed with `SURPRISED` beforehand succeeds afterward.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB.
- `npm test` in `packages/server` (live DB, freshly reseeded): 730/731 pass (723 prior + 7 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision noted since Phase 1, still untouched).

**Not done / stopped for:**
- **Surprise detection is not automated** — the DM decides who's surprised (Stealth vs. passive Perception, per the source rule) using this app's existing dice-roll tools, then supplies the roster to `startCombat` directly. No new endpoint compares rolls automatically; matches this project's established "DM adjudicates" precedent for anything requiring judgment calls this app can't fully model (weapon proficiency in P1-6, swap timing in P1-7).
- **No frontend wiring** — `packages/web` has no UI yet for the DM to select surprised participants before starting combat; the existing "Start combat" button's mutation call is untouched (posts no body, which now defaults `surprisedParticipantIds` to `[]`, i.e. today's behavior is unchanged until the UI is built). Matches every other phase this cycle (P1-6/P1-7/P1-8) — server-only, by the same scope boundary.
- **2014's "can't take a reaction" window technically starts at the moment of ambush, not at `startCombat`** — since this app has no pre-combat state to hook (the encounter isn't in initiative order yet), the lockout only takes effect once `startCombat` actually runs, which in practice IS "the start of combat" for this app's model. Not expected to matter in play, flagged for completeness.

**P1-10: Implement Cover in AC computation.**
- Acceptance criteria: an attack roll against a target with a specified cover level applies the correct AC/Dex-save bonus (+2 Half, +5 Three-Quarters); Total Cover blocks targeting entirely.
- Rule itself is unambiguous and confirmed by this project's own verified rules reference (`.claude/skills/dnd-2024-rules/references/combat.md` line 48, `rules-glossary.md` line 40): Half +2 AC/Dex saves, Three-Quarters +5 AC/Dex saves — the SAME number for both, only the best applicable degree ever applies, Total can't be targeted at all. No 2014/2024 contradiction this time (unlike P1-9).
- **The real scope question, confirmed with the user via AskUserQuestion first**: this codebase has no server-side attack hit/miss resolution anywhere — `dice_rolls` (roll_type='attack') carries no target reference and never compares its result against a target's AC; the DM/players currently eyeball the roll against the target's sheet AC themselves. So "applies the bonus" and "blocks targeting" had no existing concrete place to attach to. Two real options: (a) track Cover as DM-set per-participant state and surface a computed effective-AC/blocked flag for manual adjudication (matches this project's dominant "track state, suggest a value, let the DM adjudicate" pattern — Surprise, weapon mastery, `computeSaveDc`), or (b) additionally wire real enforcement into the attack dice-roll endpoint itself. **User chose (a)** — tracked state + computed value, no new enforcement on the core roll pipeline.
- Schema: `db/migrations/1784269836666_add-participant-cover.ts` — one column, `combat_participants.cover TEXT NOT NULL DEFAULT 'none' CHECK (IN ('none','half','three_quarters','total'))`. One value (not a set of cover sources) directly matches the "only the best degree ever applies" rule — the DM just sets whichever single degree currently applies, same shape as the existing `faction` column.
- `services/encounters.ts`'s new pure helpers `coverAcBonus(cover)` (0/2/5, with `'total'` returning 0 since it's an absolute block, not a "+X") and `coverBlocksTargeting(cover)` (true only for `'total'`) — same "never server-enforced, suggest a value" precedent as `diceEngine.ts`'s `computeSaveDc`/`proficiencyBonusForLevel`.
- `setParticipantCover` (new service function) + `PATCH /encounters/:id/participants/:pid/cover` (DM-only), mirroring the existing `setParticipantFaction`/`setParticipantHpVisibility` shape exactly (same transaction pattern, same `sync_seq` bump, same `broadcastFullStateResync` — cover changes what every viewer's snapshot reports, not just a display detail).
- `getEncounterCombatSnapshot` now derives `armor_class_effective` (base AC + cover bonus), `cover_ac_bonus`, and `cover_blocks_targeting` for every participant, computed once in JS right after the query rather than at each consumer. Threaded through to the socket wire payload (`sockets/broadcast.ts`'s `buildFullStateSyncPayload`) as `cover`/`coverAcBonus`/`armorClassEffective`/`coverBlocksTargeting` — visible to every viewer, same "AC/effects are always-visible, no redaction" precedent the base `armorClass` field already has.
- Tests: `services/encounters.cover.integration.test.ts` (new, 8 cases, live DB, throwaway fixtures) — the 4 pure bonus/blocking values; default `none` reports the plain base AC and `cover_blocks_targeting: false`; Half/Three-Quarters correctly raise `armor_class_effective` by 2/5; Total reports `cover_blocks_targeting: true`; reverting to `none` restores the plain base AC; a bad participant id 404s.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB.
- `npm test` in `packages/server` (live DB, freshly reseeded): 738/739 pass (730 prior + 8 new; the 1 failure is the same pre-existing, unrelated `campaignClasses.integration.test.ts` collision noted since Phase 1, still untouched).

**Not done / stopped for:**
- **No enforcement on attack rolls or any other "targeting" endpoint** — Total Cover's "can't be targeted" is reported (`cover_blocks_targeting`) but nothing server-side currently rejects an action against a totally-covered participant, per the user's explicit scope choice. `resolveWeaponMasteryTrigger` (P1-6), `applyEncounterEffect`/`applyDamage`, and the attack dice-roll endpoint are all untouched.
- **No frontend wiring** — `packages/web` has no UI yet for the DM to set a participant's cover degree or for the effective-AC/blocked figures to display. Server-only, same scope boundary as every other phase this cycle.
- **Cover from map geometry/terrain is not auto-derived** — this app's existing wall/light map-element system (`services/movement.ts`, `domain/vision.ts`) isn't consulted to compute a suggested cover degree from a token's actual position; the DM sets the degree by hand, same "DM adjudicates" treatment already applied to weapon proficiency (P1-6) and surprise detection (P1-9).

**P1-11: Implement Rage-style temporary resistance.**
- Acceptance criteria: damage resistance sourced from an active, non-permanent effect (e.g. Rage) applies automatically while that effect is present and stops automatically when it ends, without touching the permanent resistance columns.
- **This project already had a precise, pre-written design for this exact gap** — `docs/rules/attacks-and-damage.md` §2.4 and `OPEN_QUESTIONS.md` item 7 (both predating this roadmap) had already specified: permanent/build-derived resistance lives in `characters.damage_resistances` (already built, along with `computeAppliedDamage`/`applyDamage`/`applyMonsterInstanceDamage` — everything through §2.3 of that doc was already implemented before this phase); a temporary source like Rage should instead be a field on the active `effect_definitions` template, unioned with the permanent columns at read time by `computeAppliedDamage`'s caller, re-read fresh on every damage application, never written into the permanent columns. This phase implements exactly that already-specified design — no new design decisions or user scope questions needed, unlike P1-6/P1-9/P1-10.
- Schema: `db/migrations/1784269837666_add-effect-grants-resistance.ts` — one column, `effect_definitions.grants_resistance TEXT[] NOT NULL DEFAULT '{}'`.
- Seed: a new "Raging" template in `db/seeds/catalog.ts`'s non-condition effect-templates array, `grantsResistance: ['bludgeoning', 'piercing', 'slashing']`, text verbatim from this project's own already-seeded Barbarian "Rage" `class_features` row ("Damage Resistance. You have Resistance to Bludgeoning, Piercing, and Slashing damage."). `until_removed` duration, matching every other class-feature-driven toggle already seeded (Dodge/Hidden/the P1-6 Weapon Mastery templates) — Rage's real duration rules (ends after 1 minute of inactivity, or early on specific triggers) need turn-boundary tracking out of scope here; the player/DM ends it manually via the existing effect-remove flow.
- `services/characters.ts`'s and `services/monsters.ts`'s new `fetchActiveGrantedResistances` (one small, near-identical function per file — self-contained per service file, same precedent `armorClass.ts`'s `abilityModifier` already established, and avoids a `characters.ts`↔`effects.ts` import cycle since `effects.ts` already imports FROM `characters.ts`): queries the target's currently-active (`removed_at IS NULL`) effects joined to their definitions, unions every `grants_resistance` array. Wired into `applyDamage`/`applyMonsterInstanceDamage` right before their existing `computeAppliedDamage` call — `resistances` becomes `[...new Set([...permanentResistances, ...grantedResistances])]`, read inside the same locked transaction as the rest of the damage application, so a Rage ending between two attacks in the same round can never leave a stale resistance applied (docs/rules/attacks-and-damage.md §3 edge case 8).
- Applies to monster instances too, not just PCs — Rage is the motivating example, but the mechanism (any active effect granting a temporary resistance) also covers a monster under a temporary Resistance-granting buff, with no PC-specific assumption baked in.
- Tests: `services/damage.rageResistance.integration.test.ts` (new, 5 cases, live DB, throwaway fixtures, using the real seeded "Raging" template) — a Raging character resists bludgeoning while the effect is active, with the permanent `damage_resistances` column confirmed untouched before and after; a damage type Raging doesn't grant (fire) is unaffected; removing the effect stops the resistance on the very next damage application; a permanent resistance and a temporary one to the SAME type union without double-applying (halved once, not twice); a monster instance with the same active effect resists the same way.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB; `npm run seed` re-run cleanly, confirmed via direct SQL: the "Raging" template carries `grants_resistance = ['bludgeoning','piercing','slashing']`.
- `npm test` in `packages/server` (live DB, freshly reseeded): 744/744 pass (739 prior + 5 new).

**Not done / stopped for:**
- **Rage's real duration/end triggers aren't modeled** — the effect is `until_removed`, ended manually by the player/DM, not automatically after 1 minute of inactivity or on the other 2024 end conditions (dropping to 0 HP, donning Heavy armor, etc.). Matches the already-accepted precedent for every other class-feature toggle without a turn-boundary trigger this app tracks (Dodge, Hidden, P1-6's Weapon Mastery templates).
- **No auto-grant/auto-apply of "Raging" when a Barbarian activates Rage** — the player/DM applies the effect manually via the existing effects endpoints, same as any other active effect in this app; nothing hooks a "Barbarian enters Rage" action (which doesn't itself exist as a tracked action) to automatically insert this row.
- **No frontend wiring** — `packages/web` has no dedicated UI cue distinguishing a temporary, effect-granted resistance from a permanent one beyond what already renders for any active effect. Server-only, same scope boundary as every other phase this cycle.

**P1-12: Wire up `half_on_save`.**
- Acceptance criteria: a save-based attack marked `half_on_save` automatically halves damage when the target's save succeeds, rather than requiring manual DM computation.
- `character_attacks.half_on_save` (and monsters' `statBlockEntrySchema` equivalent) already existed as a column/field, but `services/weaponMastery.ts`'s own prior comment named the exact gap this phase closes: "saveDc/half_on_save are suggest-to-the-UI, not server-enforced" (`services/diceEngine.ts`'s `computeSaveDc` convention). This phase changes that specifically for damage application — `saveDc` itself stays a manually-entered flat integer (unchanged), but a successful save against it now actually halves/negates the applied damage instead of just being displayed.
- Rule source: `docs/rules/attacks-and-damage.md` §1.7/§2.1/§3 edge case 7 — half-on-save is round-down, and (by this project's own already-recorded inference from the general ordering rule) applies to the raw total BEFORE resistance/vulnerability/immunity, not after.
- No migration needed — `character_attacks.half_on_save` already exists; this phase only changes `POST /characters/:id/apply-damage` and `POST /monster-instances/:id/apply-damage`'s server-side handling.
- `schemas/damage.ts`'s `applyDamageSchema` gains `saveDc` (int, 1-30, same bounds as `computeSaveDc`'s output), `savingThrowRollId` (uuid), and `halfOnSave` (boolean, optional — undefined means "halve," matching `character_attacks.half_on_save`'s own default). A `.refine` requires `saveDc` whenever `savingThrowRollId` is present. `savingThrowRollId` is never trusted for its outcome directly — same principle as `attackRollId`/`isCritical` (M3 fix, Phase pre-1): `diceRolls.ts`'s new `deriveSaveOutcomeSucceeded(pool, campaignId, savingThrowRollId, saveDc)` re-reads the actual stored `saving_throw`-type `dice_rolls` row's `result_total` and compares it to the caller-supplied DC itself, rather than accepting a client-asserted succeeded/failed boolean.
- `services/damage.ts`'s `computeAppliedDamage` (pure function) gains `savingThrowSucceeded`/`halfOnSave` on `RawDamageInput` and `savedHalved`/`savedNegated` on `AppliedDamageResult.breakdown`. Ordering: `rawTotal` (dice + modifier) → halved-or-zeroed-for-save (round down) → resistance → vulnerability → immunity-short-circuit-before-any-of-it, matching the doc's already-recorded §3 edge case 7 inference.
- `services/characters.ts`'s `applyDamage` and `services/monsters.ts`'s `applyMonsterInstanceDamage` both call `deriveSaveOutcomeSucceeded` alongside their existing `deriveIsCriticalFromAttackRoll` call, and — per `docs/rules/attacks-and-damage.md` §3 edge case 6 ("save-based attacks never crit") — force `isCritical` to `false` whenever `savingThrowRollId` is present, rather than trusting a caller to never send both `attackRollId` and `savingThrowRollId` together on the same request.
- Tests: `services/damage.test.ts` gains a `P1-12` describe block (8 unit cases — halve/negate/fail/no-save-unchanged/ordering-vs-resistance/ordering-vs-vulnerability/negate-vs-vulnerable/immunity-still-wins); `services/characters.applyDamage.integration.test.ts` gains a `P1-12` describe block (6 live-DB cases — success halves, failure applies full, `halfOnSave: false` negates, a save-based hit can't crit even if `attackRollId` is also supplied, half-then-resistance ordering, a `savingThrowRollId` from a different campaign is treated as no-save); `services/monsters.halfOnSave.integration.test.ts` (new, 3 cases) proves parity on the monster-instance path without re-running the whole matrix, same "one parity file, not a duplicated matrix" precedent `damage.rageResistance.integration.test.ts` already set for its own monster-instance case.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 760/760 pass (744 prior + 8 unit + 6 character-integration + 3 monster-integration = 17 net new after some pre-existing test-count drift in `damage.test.ts`'s baseline; the previously-flagged `campaignClasses.integration.test.ts` collision did not reproduce this run).

**Not done / stopped for:**
- **No frontend wiring** — `packages/web` has no UI yet to roll a target's saving throw and feed its `dice_rolls` id + the effect's `saveDc`/`halfOnSave` into the damage-application call. Server-only, same scope boundary as every other phase this cycle.
- **`saveAbilityIndex` is still not server-checked against which ability was actually rolled** — `deriveSaveOutcomeSucceeded` only confirms the roll is `roll_type = 'saving_throw'` and reads its `result_total`; it doesn't verify the roll's `roll_context` names the same ability the triggering `character_attacks`/monster-action row's `saveAbilityIndex` expects (e.g. a Dex save being satisfied by a stored Con save roll). `dice_rolls` has no structured ability-index column to check against today (`roll_context` is free-text display-only) — flagging as a real, narrower gap rather than silently assuming it's covered.

**P1-13: Implement the Hide action.**
- Acceptance criteria: a character meeting the hiding preconditions (Heavily Obscured, or behind Three-Quarters/Total Cover, out of every enemy's line of sight) can take a Hide action resolving a DC 15 Dex(Stealth) check; success applies an Invisible-while-hidden state that ends on the documented break conditions.
- Rule source: `rulesGlossary.md` "Hide [Action]" (line 954-960) — DC 15 Dex(Stealth); success grants the Invisible condition while hidden; "make note of your check's total, which is the DC for a creature to find you"; stops immediately on: a sound louder than a whisper, an enemy finds you, an attack roll, or casting a spell with a Verbal component.
- **Preconditions are DM-adjudicated, not server-enforced** — the rule text's own framing settles this without needing a fresh scope call: "The Dungeon Master decides when circumstances are appropriate for hiding." Combined with the fact that "Heavily Obscured" has zero tracked state in this app at all (ER-09, still Part/Miss) while Cover (P1-10) is itself only ever a suggested value, no server-side precondition set could ever be complete anyway — matches Cover's own "track state, don't gate the core roll pipeline" precedent exactly, so this endpoint just resolves the check.
- **The 4 break conditions needed a scope call** (confirmed with the user via AskUserQuestion): only "you make an attack roll" has a clean, unambiguous, already-existing hook (`dice_rolls`' `roll_type = 'attack'`); the other 3 (noise, being found, a Verbal-component spell cast) need DM judgment or a source-type distinction this app can't currently draw correctly (the Invisibility SPELL's own break condition is "casts ANY spell," not just a Verbal one — conflating the two would be a real correctness bug, not a simplification). User chose: auto-clear on attack roll only; the other 3 stay DM-manual via the existing `DELETE /effects/:id` flow, matching Dodge's own precedent for anything without a clean trigger.
- No new tables. `services/hide.ts` (new) mirrors `shove.ts`/`grapple.ts`'s PC-actor-only, DM-approval-gated, action-economy-spending shape almost exactly, simplified since there's no opposed/defender side: DC is a fixed 15 (never overridden — unlike `defenderRollOverride`, the actor's own roll is never client-overridable, matching the "RNG lives here and only here" invariant), and on success the effect targets the ACTOR (self), not an opponent. Computes the Dex(Stealth) check modifier server-side from `characters.dex`, `character_skill_proficiencies` (proficient/expertise), and `character_classes`' summed total level via `diceEngine.ts`'s existing `proficiencyBonusForLevel`. On success, applies the real seeded "Invisible" `effect_definitions` row (linked to the `invisible` SRD condition — the same one CB-07's condition catalog already has) to the actor via `applyEncounterEffect`, with `notes` recording the check's total as the DC to find them (rulesGlossary.md's own instruction).
- Route: `POST /encounters/:id/participants/:pid/hide`, `requireOwnParticipantOrDm`-gated, mirroring Shove/Grapple's routes exactly including the Phase 4 pending-approval branch. `pendingActionKind` gains `'hide'` (`db/migrations/1784269838666_add-hide-pending-action-kind.ts` — one `ALTER TABLE ... DROP/ADD CONSTRAINT`, the auto-named check confirmed against the live dev DB via `pg_get_constraintdef` before writing it, same discipline the CB-02 theme-widening migration used); `routes/pendingActions.ts`'s approval-dispatch switch gains a matching `case 'hide'`.
- **The attack-roll break condition** (`services/diceRolls.ts`'s `rollDice`): when an `'attack'`-type roll's `characterId`/`monsterInstanceId` has an active `'Invisible'`-named effect, it's soft-removed in the SAME transaction as the roll insert (mirrors `advanceTurn`'s existing Dodge-clear-on-turn-start discipline, including bumping the owning encounter's `sync_seq`). Deliberately clears ANY active Invisible effect regardless of source, not just a Hide-sourced one — the Invisibility SPELL's own text ("ends early if the target attacks") happens to break on the exact same trigger, so this is RAW-correct for both mechanisms at once, with no source-type branch needed for THIS specific trigger (unlike the Verbal-spell-cast trigger, which genuinely differs by source and was deliberately left out, above). `rollDice`'s return type gains an additive-only `clearedInvisibleEffect?` field; `routes/diceRolls.ts` — the only real call site that ever creates an `'attack'`-type roll (shove/grapple/weaponMastery all use `'skill_check'` or reference an existing roll, never mint a new `'attack'` roll themselves) — broadcasts `EFFECT_EXPIRED` for it when set. Every other existing `rollDice` caller is unaffected (the field is optional and ignored where unused).
- Tests: `services/hide.integration.test.ts` (new, 4 cases, live DB, throwaway fixtures, deterministic via extreme Dex scores rather than a roll override, since the actor's own roll can never be overridden) — a guaranteed-success Hide applies Invisible and records the DC in `notes`; a guaranteed-failure Hide spends the action but applies nothing; a second Hide attempt with the action already used is rejected; an out-of-turn Hide attempt is rejected with `NOT_YOUR_TURN`. `services/diceRolls.clearInvisibleOnAttack.integration.test.ts` (new, 4 cases, using the real seeded "Invisible" template) — an attack roll clears an active Invisible effect on a character; a non-attack roll (skill check) leaves it untouched; an attack roll with no active Invisible effect is a clean no-op; a monster instance's Invisible effect clears the same way.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB.
- `npm test` in `packages/server` (live DB, freshly reseeded): 768/768 pass (760 prior + 4 Hide + 4 clear-Invisible).

**Not done / stopped for:**
- **Hide preconditions aren't checked at all server-side** — by design, per the rule text's own DM-discretion framing (above); a click of the Hide button is treated as the DM/player's own assertion the circumstances are met, same as every other action this cycle that stops short of full geometric/state automation.
- **3 of the 4 break conditions stay DM-manual** — loud noise, being found (a Perception-vs-the-recorded-DC check this app doesn't automate, matching Surprise detection's own precedent), and casting a spell with a Verbal component. The DM/player removes the effect via the existing `DELETE /effects/:id` flow.
- **Hide is scoped to PC actors only** (`combat_participants.character_id`-backed), matching Shove/Grapple's own narrowing — an NPC/monster-instance-initiated Hide (e.g. a DM setting an ambush) isn't wired to this endpoint; the DM can still track it manually via the generic effects endpoint.
- **No frontend wiring** — `packages/web` has no UI yet for a Hide button, the check-result toast, or a visible "Invisible (Hidden)" badge distinct from other Invisible sources. Server-only, same scope boundary as every other phase this cycle.
- **Feature-driven modifiers to the Hide check itself aren't applied** (e.g. the 2024 Rogue subclass feature granting Advantage on this specific check during combat, or `Naturally Stealthy`'s obscurement exception) — the roll is always `keep: 'normal'`, matching the already-accepted "swap/feature-application depth not enforced" precedent from P1-6's Cleave/Nick gaps.

## Phase 3 — P2 completeness (done)

**P2-1: Implement the Incapacitated occupancy-passability exception (CB-01).**
- Acceptance criteria: a 2024-edition creature can pass through an Incapacitated occupant's space (difficult-terrain rate, same as the existing ally/Tiny/2-sizes-different exceptions), closing the gap `services/movement.ts`'s own header comment and `OPEN_QUESTIONS.md` item 6 both flagged since the original movement phase.
- Rule source: `rulesGlossary.md` combat.md lines 59-64 ("You can pass through an ally's space, an Incapacitated creature's space, a Tiny creature's space, or a creature two sizes larger/smaller than you") — 2024-only; 2014's occupancy rule has no such exception (confirmed in `docs/rules/movement.md` §1.7/§3 edge case 8).
- **The "is this participant currently Incapacitated" predicate** (the actual gap — `active_effects` stores applied conditions by name, not a structured boolean) is resolved by matching active effect names against the 5 conditions whose OWN 2024 text composes "you have the Incapacitated condition" — confirmed directly against `rulesGlossary.md` rather than guessed: Incapacitated itself, Paralyzed (line 1223), Petrified (line 1255), Stunned (line 1543), Unconscious (line 1653). No other condition's text includes this composition (checked every condition's own section for the phrase).
- `services/movement.ts`'s `Occupant` interface gains `isIncapacitated: boolean`, and `isOccupantPassable`'s 2024 branch becomes `sameSide || occupant.sizeRank === 0 || occupant.isIncapacitated` — the module stays trait-agnostic per its own established convention (caller-supplied boolean, never looks up effects itself, same principle `diceEngine.ts`'s header comment states for game-feature eligibility).
- `services/encounters.ts`'s `loadMovementContext` (shared by both `setParticipantPosition`'s validation path and the reachable-cells read endpoint — one fix covers both) now computes `is_incapacitated` per occupant row via a correlated `EXISTS` subquery against `active_effects`/`effect_definitions`, same shape as that function's existing `is_prone` check for the mover, just per occupant instead of for one participant.
- No migration needed — reads the same `active_effects`/`effect_definitions` tables every other condition-aware feature this cycle already uses.
- Tests: `services/movement.test.ts` gains 2 new pure-function cases (2024 Incapacitated exempts a same-size hostile occupant; 2014 does NOT exempt it — same occupant, same size, edition is the only variable) plus `isIncapacitated: false` added to the 7 pre-existing occupant fixtures (`Map.set` object literals — test files are excluded from `tsc -b`'s strict compilation per this package's own `tsconfig.json`, so these were still runtime-correct beforehand via `undefined` acting as falsy, but were made explicit for clarity). `services/encounters.incapacitatedOccupancy.integration.test.ts` (new, 3 cases, live DB, a 1-row/3-column corridor map so there's no cheaper diagonal detour Dijkstra could take around a blocked occupant, mirroring `movement.test.ts`'s own difficult-terrain-test trick) — a same-size hostile occupant blocks the path outright before any effect is applied; applying the real seeded "Stunned" effect makes it passable at the difficult-terrain rate (`movement_used_ft` matches the exact math the pure-function test predicts); removing the effect blocks the path again for a fresh mover.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 773/773 pass (768 prior + 2 movement.test.ts + 3 incapacitatedOccupancy integration).

**Not done / stopped for:**
- **No frontend wiring** — `packages/web` has no UI cue distinguishing "this occupied cell is passable because the occupant is Incapacitated" from the existing ally/Tiny cases. Server-only, same scope boundary as every other phase this cycle.
- **The 2024 "gain Prone if forced to end your move in an occupied space" consequence** (`docs/rules/movement.md` §1.3, confirmed-by-cross-edition-consistency) is still not implemented — this endpoint only affects whether a cell is a legal *waypoint*, not what happens if a move is somehow forced to *end* inside one (nothing in this app currently produces that forced-end scenario). Out of scope for this item, not silently assumed solved.

**P2-2: Auto-apply mechanical consequences of tracked conditions in attack/damage resolution (CB-07).**
- Acceptance criteria (roadmap text, as written): conditions tracked as applied `active_effects` rows should have their mechanical consequences (Prone→melee advantage, Restrained→attack/save disadvantage, etc.) auto-applied in attack/damage resolution, rather than staying inert flavor state.
- **Real scope question, confirmed with the user via AskUserQuestion first** — this app has NO server-side attack hit/miss resolution pipeline at all: `dice_rolls` (`roll_type='attack'`) carries no target reference and never compares its result against anything, matching the exact wall P1-10 (Cover) already hit and documented ("no server-side attack hit/miss resolution anywhere... the DM/players currently eyeball the roll"). "Auto-apply in attack resolution" therefore has no existing pipeline to hook into. Three options were on the table: (a) compute-and-suggest only (report what conditions imply, DM/player applies `keep` themselves — matches Cover's/Surprise-detection's own precedent exactly), (b) real enforcement (add a `targetParticipantId` to attack rolls, schema+migration, server-computed `keep`) for just Prone/Restrained, or (c) the same real enforcement generalized to every condition with an attack/save effect (~9-10 conditions). **User chose (a)** — compute-and-suggest only, no schema change, no enforcement on the roll pipeline.
- Rule source: every constant in the new pure module is sourced directly from its own `rulesGlossary.md` condition entry (2024 SRD) — see that module's own per-constant citations rather than re-deriving them here. Conditions covered end-to-end (unconditional or clearly-scoped effects): Blinded, Invisible, Paralyzed, Petrified, Poisoned, Prone, Restrained, Stunned, Unconscious. Conditions with a real but genuinely CONTEXT-DEPENDENT effect (Frightened's "while the fear source is in line of sight," Grappled's "against any target other than the grappler," Blinded/Deafened's "only for a check requiring sight/hearing") are surfaced as human-readable **caveats**, never folded into the unconditional advantage/disadvantage lists — this function can't resolve WHO the fear source or grappler is, or WHICH ability check is being made, so guessing would be actively wrong more often than a flagged caveat. Charmed/Deafened have no attack-roll/save effect at all and are correctly excluded from those categories.
- `services/conditionEffects.ts` (new, pure, no DB — same "pure-function-first" precedent as `damage.ts`/`diceEngine.ts`/`movement.ts`): `computeOwnAttackRollModifiers`, `computeAttacksAgainstMelee`/`computeAttacksAgainstRanged` (Prone is the one condition whose effect flips on range, so it gets its own melee/ranged split rather than a single ambiguous list), `criticalHitSourcesWithin5ft` (Paralyzed/Unconscious), `computeAbilityCheckModifiers`, `computeSavingThrowModifiers` (per ability — Paralyzed/Stunned/Unconscious/Petrified auto-fail Str+Dex only; Restrained disadvantages Dex only, never auto-fails), and `computeExhaustionPenalty` (2024's flat `-2 × level` on every D20 Test, rulesGlossary.md line 834 — 2014's structurally different per-level effects table is explicitly NOT modeled, flagged below rather than guessed).
- `services/effects.ts` gains the DB-aware orchestration: `getCharacterConditionEffects`/`getMonsterInstanceConditionEffects` look up the target's currently-active conditions (filtered to `effect_definitions.condition_id IS NOT NULL` — excludes non-condition templates like Dodge/Raging/Invisible-from-Hide's own sibling templates from muddying the report) and exhaustion level (`characters.exhaustion_level` column for PCs; an active "Exhaustion" effect's `stack_count` for monster instances, which have no dedicated column — same mechanism this project's own seed comment already documented for that condition), then hand off to the pure module. Same "any campaign member may read" convention as the existing `listCharacterEffects`/`listMonsterInstanceEffects` this sits beside.
- Routes: `GET /characters/:id/condition-effects`, `GET /monster-instances/:id/condition-effects` — read-only, no request body, siblings to the existing `GET .../effects` routes.
- Tests: `services/conditionEffects.test.ts` (new, 19 pure-function cases) — every condition's unconditional list membership, Prone's melee/ranged flip specifically (including combined with another unconditional-Advantage condition, proving no double-counting), Frightened/Grappled/Blinded/Deafened surfacing as caveats only, exhaustion's `-0`-vs-`0` normalization (`Object.is` via `toBe` caught this: `-2 * 0` is `-0` in JS). `services/effects.conditionEffects.integration.test.ts` (new, 6 cases, live DB) — a clean character reports empty everywhere; a real Prone+Poisoned character reports correctly across every category at once; `exhaustion_level` drives the penalty independent of other conditions; a monster instance's Exhaustion `stack_count` path; Restrained's Dex-only disadvantage (never auto-fail); a non-condition effect (Dodge) is excluded from the conditions list entirely.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 798/798 pass (773 prior + 19 pure + 6 integration).

**Not done / stopped for:**
- **Nothing is enforced** — by the user's own explicit choice (above), this is purely advisory. A player/DM must still read the report and choose `keep` on their own roll; no attack roll, save, or ability check is ever auto-modified.
- **Charmed's "can't attack the charmer" is out of scope** — it's a hard targeting restriction, not an advantage/disadvantage roll modifier, and would need to know WHO the charmer is (this function only ever receives a flat condition-name list, no source-creature linkage) — a structurally different feature from everything else in this report.
- **Petrified's blanket damage resistance and Poisoned-immunity aren't wired into `computeAppliedDamage`** — folding those into the ACTUAL resistance arrays `applyDamage`/`applyMonsterInstanceDamage` read would be real enforcement (changing computed damage), not advisory, so it was deliberately left out of this "suggest only" pass even though the data (Petrified's presence) is already visible in the report's `conditions` array.
- **2014's Exhaustion is not modeled** — `computeExhaustionPenalty` returns 0 for any 2014 campaign; that edition's per-level effects table (disadvantage on specific roll types by level, HP max halving, speed reduction — not a single flat numeric penalty) is a genuinely different, separable mechanic this pass didn't build.
- **No frontend wiring** — `packages/web` has no UI yet to call these endpoints or surface the report next to a roll. Server-only, same scope boundary as every other phase this cycle.

**P2-3: Auto-detect Opportunity Attack triggers (CB-08).**
- Acceptance criteria: leaving a threatened square without Disengaging should auto-prompt a Reaction, closing the gap `services/encounters.ts`'s own `applyActionEconomy` comment already named ("reactions carve-out exists; no trigger logic").
- Rule source: `rulesGlossary.md` line 1215 — "You can make an Opportunity Attack when a creature that you can see leaves your reach using its action, its Bonus Action, its Reaction, or one of its speeds," and line 794 — Disengage: "your movement doesn't provoke Opportunity Attacks for the rest of the current turn."
- **"Compute-and-suggest" only** — same precedent as P2-2/CB-07 and P1-10/Cover, not re-confirmed with a fresh question this time since the philosophy is now well-established this cycle: this app has no attack resolution pipeline to force a Reaction spend against, so this reports WHICH threat sources a move left the reach of; the DM/player decides whether to actually spend the Reaction via the pre-existing `applyActionEconomy(spend: 'reaction')` path (unchanged).
- **A real, necessary sub-decision**: to avoid the feature being actively wrong (flagging a trigger for an enemy who legally can't take it), a Disengaging creature's own moves must never trigger anything, and a threat source that's already spent its Reaction or is Incapacitated must be excluded — this needed one small new piece of tracked state this app didn't have: `services/encounters.ts` had no way to know "did this participant's action get spent specifically on Disengage" (distinct from any other action). Solved the same way Dodge already solves the identical problem for itself: a new "Disengaging" `effect_definitions` template (seed addition only, no migration — mirrors "Dodge" exactly), cleared by the SAME `advanceTurn` code block that already clears Dodge on the participant's own next turn starting (generalized from a single `ed.name = 'Dodge'` check to `ed.name IN ('Dodge', 'Disengaging')`) — functionally equivalent to "the rest of the current turn" since this app never lets a participant move again between their own turn ending and their own next turn starting.
- Reach is a flat 5 ft for every threat source (`rulesGlossary.md` "Reach": "5 feet unless a rule says otherwise" — this app tracks no per-creature/per-weapon reach override, so the stated default is the correct value, not a simplification), measured as **Chebyshev (grid) distance**, not Euclidean — matching this module's own diagonal-step-cost model (a diagonally-adjacent cell is "1 step"/5ft away under the 'flat' rule, the same convention `domain/vision.ts`'s Euclidean `distance` deliberately does NOT use, since that one answers a different, non-grid-snapped vision-range question).
- `services/movement.ts`'s new `computeOpportunityAttackTriggers(path, moverFaction, feetPerCell, threats)` (pure, no DB): walks the mover's already-computed path (from `computePathCost`, previously discarded after extracting `costFt` — now kept), and for each threat source, fires once if the mover transitions from within-reach to outside-reach at any step (never more than once per threat source per move, even if the path re-enters and re-leaves reach). Only opposing sides trigger (`sideOf`'s existing party/foes/neutral split, reused as-is — neutral never triggers either direction, matching this module's already-established "neutral is nonhostile to everyone" reading for the occupancy rule).
- `services/encounters.ts`'s `loadMovementContext` gains one more `is_disengaging` `EXISTS` subquery (mirrors the existing `is_prone` one exactly) and extends the ALREADY-QUERYING-every-other-positioned-participant `occupants` query to also select `cp.id`/`cp.reaction_used`, building a parallel `threats: ThreatSource[]` array from the SAME rows — no second query needed, since P2-1's occupancy query already loads faction/position/`isIncapacitated` for every candidate. `computeValidatedMoveCost` short-circuits to an empty trigger list when the mover is Disengaging, otherwise calls the pure function; its return type (and `setParticipantPosition`'s, via a new `SetParticipantPositionResult` extending the shared `ParticipantMutationResult` rather than adding an irrelevant field to every other participant mutation) now carries `opportunityAttackTriggers: string[]`.
- Route: `PATCH /:id/participants/:pid/position`'s existing response gains `opportunityAttackTriggers` alongside `participant` — response-only, not threaded through the `TOKEN_MOVED` socket broadcast (that payload already has its own DM/player visibility split and vision-resync branching; extending it was a materially bigger lift than this item's scope, flagged below rather than silently built).
- Tests: `services/movement.test.ts` gains a `computeOpportunityAttackTriggers` describe block (10 pure-function cases) — the core leaves-reach transition, staying-within-reach never triggering, never-entering-reach never triggering, same-side exclusion, neutral exclusion (both directions), `canReact: false` exclusion, at-most-one-trigger-per-threat across a re-enter/re-leave path, multiple independent threats from one move, and the Chebyshev-vs-Euclidean diagonal case specifically (a diagonally-adjacent threat at true distance ~7ft still counts as in-reach). `services/encounters.opportunityAttack.integration.test.ts` (new, 4 cases, live DB, a 2-row map with the threat off the mover's own path so occupancy passability never confounds the result) — a far move from adjacent-to-an-enemy triggers; a short move that stays in reach doesn't; a Disengaging mover never triggers even on the identical far move; an enemy that already spent its Reaction never triggers.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run seed` re-run cleanly (effect_definitions: 23 → 24, confirmed the new "Disengaging" row via direct SQL).
- `npm test` in `packages/server` (live DB, freshly reseeded): 812/812 pass (798 prior + 10 movement.test.ts + 4 opportunityAttack integration).

**Not done / stopped for:**
- **Nothing is enforced** — purely advisory, matching P2-2's own explicit scope. No Reaction is ever auto-spent, no attack is ever auto-rolled; the DM/player still chooses whether and how to resolve the Opportunity Attack via the existing manual reaction-spend path.
- **Not broadcast to other connected clients** — a player's move that triggers an Opportunity Attack is only visible in that same request's own HTTP response; a DM watching a different browser tab doesn't get a live prompt (would need `opportunityAttackTriggers` threaded through `TOKEN_MOVED`'s payload and its existing visibility/vision-resync branching — a bigger, separable lift). The DM sees it immediately today only when the DM is the one who issued the move.
- **Reach is always flat 5 ft** — no per-creature/per-weapon reach override (e.g. a Reach weapon's 10 ft, or a Large+ creature's innate reach), since this app tracks no such data anywhere yet; would need new schema, out of scope here.
- **A creature the mover can't see never gets exempted** — rulesGlossary.md's trigger text requires "a creature that you CAN see," but this app has no per-observer visibility/line-of-sight check wired into this path (the closest existing machinery, `domain/vision.ts`'s `computeVisibleParticipantIds`, answers a different question — which participant ROWS a given player's socket connection receives — not "can participant A see participant B" as a general predicate). An Invisible or otherwise-unseen threat source is still reported as a trigger.
- **No frontend wiring** — `packages/web` has no UI yet to surface `opportunityAttackTriggers` after a move or prompt the DM to resolve it. Server-only, same scope boundary as every other phase this cycle.

**P2-4: Auto-reduce Exhaustion by 1 on Long Rest completion (ER-03).**
- Acceptance criteria: `performRest`'s long-rest branch should reduce `characters.exhaustion_level` by 1 (floored at 0), rather than leaving it to the separate DM-only manual `updateExhaustion` endpoint every time.
- Rule source: `rulesGlossary.md` line 838 — "Finishing a Long Rest removes 1 of your Exhaustion levels. When your Exhaustion level reaches 0, the condition ends." Short Rest is never named — confirmed by omission, not silently assumed, so a short rest deliberately leaves `exhaustion_level` untouched.
- `services/rests.ts`'s `performRest`: the per-character `SELECT ... FOR UPDATE` now also reads `exhaustion_level`; the long-rest branch computes `Math.max(0, exhaustionBefore - 1)` and writes it in the SAME `UPDATE characters SET hp_current = ..., hit_dice_remaining = ..., exhaustion_level = ...` statement HP/hit-dice already use (no extra round-trip, no separate transaction). `RestCharacterResult` gains `exhaustionBefore`/`exhaustionAfter` fields, mirroring the already-existing `hpBefore`/`hpAfter` pair exactly — always populated, equal to each other for a short rest (matches `hpAfter`'s own "unchanged outside the branch that touches it" precedent) rather than being omitted/undefined for that case.
- No migration needed — `characters.exhaustion_level` already existed (0-6, `CHECK`-constrained) as a plain column, just never read/written by `performRest` before now; the manual `PATCH` endpoint (`updateExhaustion`) is untouched and still works for a DM's own one-off adjustments (environmental exhaustion, a homebrew effect, correcting a mistake).
- Tests: `services/rests.performRest.integration.test.ts` gains an `exhaustion (P2-4/ER-03)` describe block (3 new cases, live DB) — a long rest reduces exhaustion by exactly 1; a long rest on an already-0 character stays at 0, never goes negative; a short rest leaves exhaustion completely untouched (run against the SAME character the first case already reduced to 2, proving the short-rest no-op isn't just "it happened to start at 0").

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm test` in `packages/server` (live DB, freshly reseeded): 815/815 pass (812 prior + 3 new).

**Not done / stopped for:** nothing — this item had no real scope ambiguity (unlike P2-2/P2-3, no attack-resolution-pipeline wall to hit) and no dependency gaps; monster instances have no `exhaustion_level` column of their own (per P2-2's own note, a DM tracks monster exhaustion via an "Exhaustion" `active_effects` row's `stack_count` instead), and `performRest` only ever operates on `characters` rows in the first place, so there's no monster-instance case this item could have covered but didn't.

**P2-5: Rest interruption tracking + Long Rest partial credit (ER-04).**
- Acceptance criteria (roadmap text, as written): track the 4 rulesGlossary.md interruption sources (rolling Initiative, taking damage, casting a non-cantrip spell, 1 hour of exertion) and grant Short-Rest-level benefits if an interrupted Long Rest had gone at least 1 hour first, rather than `performRest` staying an instant, unconditional bulk action with no interruption concept at all.
- **Real scope question, confirmed with the user via AskUserQuestion first**: this app has NO in-game clock anywhere (`rest_events.occurred_at` is only a real-world DB timestamp) — "1 hour of exertion" and "rested at least 1 hour before the interruption" can't be evaluated without inventing a time-tracking subsystem from scratch, well beyond this item's own effort estimate. Two options were on the table: (a) build a real "in-progress rest" state machine, auto-detecting the 2 interruption sources this app can actually see (damage, Initiative) and having the DM manually supply both the non-detectable sources (spell cast, exertion) and the elapsed-duration number the partial-credit threshold needs, or (b) build nothing new and just document that `performRest`'s existing `restType:'short'` parameter already produces the exact partial-credit outcome if the DM calls it that way. **User chose (a)**.
- Rule source: `rulesGlossary.md` line 1143 (Long Rest interruptions), line 1150 ("If you rested at least 1 hour before the interruption, you gain the benefits of a Short Rest"), line 1405 (Short Rest interruptions), line 1411 ("An interrupted Short Rest confers no benefits"), lines 1133/1397 ("must have at least 1 Hit Point" to start either rest).
- Migration (`1784269839666_add-rest-interruption-tracking.ts`): `rest_events.status` (`'in_progress' | 'completed'`, defaults `'completed'` — every existing row, and every row `performRest` still creates, is unaffected); `rest_event_characters.hp_after` becomes nullable (unknown until the rest actually completes); `rest_event_characters.interrupted_at`/`interruption_reason` — **per-character, not per-event**, since each of the 4 interruption sources is a property of ONE creature's own rest (two characters resting together can be interrupted independently).
- `services/rests.ts`: `performRest` is **completely unchanged in behavior** (still the instant, unconditional path for the common no-interruption case) — only refactored to extract its HP/hit-dice/pool/exhaustion-granting logic into a shared `applyRestBenefits` helper, so the new `completeRest` reuses the EXACT same math rather than duplicating it. New additive flow: `startRest` (creates an `'in_progress'` event + per-character rows with only `hp_before` known; rejects a 0-HP character per the "at least 1 HP to start" rule); `interruptInProgressRest` (exported, DB-level, a no-op unless the character actually has an in-progress rest — called unconditionally from the 2 auto-detectable hook sites below, so the overwhelming majority of calls correctly do nothing); `interruptRest` (DM-only, manually flags any of the 4 reasons — the only path for `'spell'`/`'exertion'`, and also usable as a correction tool for the other two); `completeRest` (DM-only, takes a DM-supplied `elapsedMinutes` — computes each character's `effectiveRestType` independently: uninterrupted → the event's own type; interrupted Long Rest with `elapsedMinutes >= 60` → `'short'` (partial credit); everything else (interrupted Short Rest at any duration, or an interrupted Long Rest under 60 minutes) → `'none'`).
- **The 2 auto-detected hooks**: `services/characters.ts`'s `applyDamage` calls `interruptInProgressRest(client, characterId, 'damage')` whenever `appliedDamage > 0` (rulesGlossary.md's "taking ANY damage" — checked pre-temp-HP-absorption, since even a hit temp HP fully soaks was still damage taken; checked post-resistance, since 0 damage from an immune/negated hit was never actually "taken"). `services/encounters.ts`'s `rollAndReorderInitiative` (the single function underlying `startCombat`/`rollInitiative`/a mid-combat join — one hook covers all three call sites) calls it with `'initiative'` for every character-backed participant who's just been freshly rolled.
- Routes: `POST /campaigns/:id/rests/start`, `POST /campaigns/:id/rests/:restId/interrupt`, `POST /campaigns/:id/rests/:restId/complete` — additive siblings to the existing `POST /campaigns/:id/rests` (`performRest`, untouched) and `GET /campaigns/:id/rests` (`listRests`, already generic `SELECT *` + join, so an in-progress event's partial `rest_event_characters` rows — including the new nullable `hp_after` and `interrupted_at`/`interruption_reason` — show up there automatically with no changes needed).
- Service-level authorization was added for real on all 3 new functions (`requireMembership` + `requireDm`, checked against `pool` BEFORE opening the mutating transaction — a `PoolClient` doesn't structurally satisfy `requireMembership`'s `Pool`-typed parameter, same split `shove.ts`/`grapple.ts`/`hide.ts` already use) — an improvement over `performRest`'s own long-standing route-only DM gate (`OPEN_QUESTIONS.md` item 8's flagged historical pattern), not copied forward into the new code.
- Tests: `services/rests.interruptible.integration.test.ts` (new, 10 cases, live DB) — `startRest` captures `hp_before` with no benefits granted yet; rejects a 0-HP character; an uninterrupted long rest via `completeRest` grants full benefits (reusing the exact same math P2-4's exhaustion test already validates); a long-rest interruption via REAL `applyDamage` at `elapsedMinutes: 90` grants Short Rest benefits only (pools reset, HP/exhaustion/hit-dice untouched, `long_rest`-recharge pool specifically confirmed NOT reset); a long-rest interruption at `elapsedMinutes: 30` grants nothing; an interrupted Short Rest grants nothing regardless of elapsed time; completing an already-completed rest throws `CONFLICT`; `interruptRest` rejects a character not part of the rest and rejects double-interruption; a REAL `startCombat` call interrupts an in-progress rest with reason `'initiative'`; `interruptInProgressRest` is a true no-op (no throw, no side effect) for a character with no in-progress rest at all.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB.
- `npm test` in `packages/server` (live DB, freshly reseeded): 825/825 pass (815 prior + 10 new).

**Not done / stopped for:**
- **"1 hour of exertion" is never auto-detected** — this app has no player-facing "declare exertion" action to hook, and no clock to time it against; only reachable via the DM's manual `interruptRest(reason: 'exertion')`.
- **"Casting a spell other than a cantrip" is never auto-detected** — `services/casting.ts` wasn't touched; hooking it would need a clean "is this specific cast a cantrip" signal threaded through to an interruption call, which wasn't in the confirmed scope (only the 2 already-tracked sources). Only reachable via `interruptRest(reason: 'spell')`.
- **`elapsedMinutes` is entirely DM-trusted** — nothing verifies it against reality (there is no reality to verify it against); a DM can claim any duration for the partial-credit threshold.
- **"Resuming a Long Rest costs 1 extra hour per interruption"** (rulesGlossary.md line 1152) is not modeled — `completeRest` is a one-shot terminal action per rest event; a DM who wants to represent "resumed after an interruption" today just starts a fresh `startRest` call, with no extra-hour bookkeeping carried over.
- **16-hours-between-Long-Rests eligibility** (rulesGlossary.md line 1131) is not enforced — same "no in-game clock" reason as above; `startRest` only checks the 1-HP-minimum precondition, which is the one eligibility rule groundable without a clock.
- **No frontend wiring** — `packages/web` has no UI yet for starting/interrupting/completing a rest as a multi-step flow (today's UI, if any, can only call the unchanged instant `performRest`). Server-only, same scope boundary as every other phase this cycle.

**P2-6: Model Lightly/Heavily Obscured distinctly, and add Blindsight/Tremorsense/Truesight as senses (ER-09).**
- Acceptance criteria (roadmap text, as written): Lightly Obscured (Perception disadvantage) and Heavily Obscured (effectively Blinded) should be modeled as distinct states rather than collapsed, and Blindsight/Tremorsense/Truesight should exist as trackable senses alongside the existing vision/darkvision radii.
- **Real scope question, confirmed with the user via AskUserQuestion first** — `domain/vision.ts` is explicitly, in its own header comment, "the actual security boundary" deciding which participant ROWS a player's socket even receives under `'dark'` map lighting; it is not a general-purpose visibility simulator. Maps already have a 3-state `lighting_state` (`'bright'/'dim'/'dark'`), but `'dim'` behaves identically to `'bright'` in every consumer today — which turns out to already be RAW-correct for THAT specific reveal-engine job, since Lightly Obscured/Dim Light never blocks whether a creature can be perceived to exist at all, only imposes Perception disadvantage on certain checks. Two options: (a) add the 3 new senses as tracked fields plus a NEW, separate advisory module reporting obscurement consequences — informational only, `computeVisibleParticipantIds` untouched — or (b) also rewire that actual reveal-engine gate to account for the 3 new senses piercing `'dark'` lighting. **User chose (a)** — the security-sensitive reveal engine stays exactly as-is.
- Rule source: every constant/branch in the new module cites its own `rulesGlossary.md` entry — Bright Light (line 425), Dim Light → Lightly Obscured (line 780), Darkness → Heavily Obscured (line 700), Lightly Obscured's Perception disadvantage (line 1105), Heavily Obscured's scoped Blinded-while-trying-to-see-into-it (line 932), Darkvision's one-step downgrade (line 708: Dim→Bright, Darkness→Dim — NOT a full negation, unlike the two senses below), Blindsight's full negation plus Invisible-piercing plus its own Total Cover exception (line 372), Truesight's full negation plus Invisible/illusion/transformation-piercing with no Total Cover exception (line 1617), Tremorsense's non-visual same-surface-contact/no-airborne-targets rule (line 1609).
- Migration (`1784269840666_add-participant-blindsight-tremorsense-truesight.ts`): `blindsight_radius_ft`/`tremorsense_radius_ft`/`truesight_radius_ft` on `combat_participants`, mirroring `vision_radius_ft`/`darkvision_radius_ft`'s own precedent exactly (DM-tunable per seating, not derived from any catalog field — this project's `senses` catalog text is free-form, no structured numeric field exists to derive a default from). `setParticipantVisionSchema`/`setParticipantVision` extended with the same optional-COALESCE-per-field shape the existing two radii already use.
- `domain/obscurement.ts` (new, pure, no DB): `obscurementFromLightLevel` (the 3-state mapping); `perceptionConsequenceOf` (Lightly → disadvantage flag; Heavily → a `effectivelyBlindedForThisTarget` flag scoped to sight checks aimed at THIS target specifically, not a blanket Blinded condition — matching line 932's own "while trying to see something in [it]" framing rather than over-applying Blinded's full effect list); `computeSightResult` (picks the best-available outcome across darkvision/blindsight/truesight for a given distance — blindsight/truesight both fully negate obscurement to `'none'` and grant Invisible-perception within range, blindsight alone respects the target's Total Cover per its own named exception since this app already tracks Cover per participant from P1-10; darkvision only downgrades one step and never pierces Invisible); `tremorsenseDetects` (a simple range+contact+not-airborne check — `viewerAndTargetShareSurfaceContact`/`targetIsAirborne` are caller-supplied booleans, since this app tracks no surface-contact/flying/z-axis state at all, matching P2-2's Frightened/Grappled "context this function can't resolve on its own" precedent).
- `services/encounters.ts`'s new `getParticipantSightReport(pool, encounterId, viewerParticipantId, targetParticipantId)` wires the pure module to real data: the encounter's active map's `lighting_state`/`feet_per_cell`, both participants' positions (Euclidean distance via `domain/vision.ts`'s existing `distance`, converted to feet), the viewer's 3 sense radii, and the target's Cover (via P1-10's existing `coverBlocksTargeting`). Returns `null` when either participant isn't placed yet or the map has no usable scale — same "nothing to compute against yet" convention `loadMovementContext`/`getParticipantReachableCells` already use, not an error.
- Route: `GET /encounters/:id/participants/:pid/obscurement?targetParticipantId=<uuid>`, `requireOwnParticipantOrDm`-gated (same as the reachable-cells route it sits beside) — a player may check what their OWN participant perceives.
- Tests: `domain/obscurement.test.ts` (new, 19 pure-function cases) — the 3-state light mapping; each obscurement level's exact, non-overlapping Perception consequence; darkvision's one-step downgrade both directions, including the "already `'none'`, source stays `'normal'` not falsely `'darkvision'`" edge case; blindsight's full negation, its Total Cover exception specifically, and Invisible-perception; truesight's full negation with NO Total Cover exception; blindsight preferred over truesight when both qualify for the identical best outcome; a sense with a shorter range correctly falling through to a longer-ranged one still in range; the 5 tremorsense edge cases (airborne exclusion, no-contact exclusion, out-of-range exclusion, zero-radius). `services/encounters.obscurement.integration.test.ts` (new, 7 cases, live DB) — the 3 light levels against a real 2-participant/real-map fixture at a fixed real distance; Darkvision's downgrade and Blindsight's full negation via REAL `setParticipantVision` calls; Blindsight blocked by REAL Total Cover via `setParticipantCover`; `null` returned for an unplaced participant.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npm run migrate` applied cleanly against the live dev DB.
- `npm test` in `packages/server` (live DB, freshly reseeded): 851/851 pass (825 prior + 19 new pure + 7 new integration).

**Not done / stopped for:**
- **`domain/vision.ts`'s actual reveal-engine (`computeVisibleParticipantIds`) is untouched** — by the user's own explicit choice (above). A player's socket-level fog-of-war under `'dark'` lighting still only ever considers `darkvisionRadiusFt`; Blindsight/Tremorsense/Truesight have no effect on what a player's client actually receives, only on this new advisory report.
- **Per-cell/regional light levels are still not modeled** — `lighting_state` remains one value for the WHOLE map (a pre-existing design choice, not something this item changed); a torch-lit room next to a dark corridor on the same map can't be represented, so the obscurement report is only as accurate as "the whole map is uniformly lit."
- **Tremorsense's surface-contact/airborne inputs are always DM-supplied** — no flying/climbing/z-axis state exists anywhere in this app to derive them from automatically.
- **No frontend wiring** — `packages/web` has no UI yet to call the new endpoint or surface blindsight/tremorsense/truesight radii in the vision-config panel. Server-only, same scope boundary as every other phase this cycle.

**P2-7: Surface trinkets in the character creation wizard (CC-04).**
- Acceptance criteria (roadmap text, as written): "Trinkets — catalog data seeded but never surfaced in the character creation wizard" (`db/seeds/catalog.ts` has trinket data; zero references anywhere under `packages/web/src`) — `character-creation.md` line 23 ("one free trinket").
- **Real scope question, confirmed with the user via AskUserQuestion first**: the ticket's own premise was checked and found false — exhaustive grep across `db/seeds/catalog.ts`, every migration, and the SRD source JSON confirmed NO trinket data has ever existed anywhere in this project; the roadmap doc's claim of already-seeded-but-unsurfaced data was simply wrong. Two options were on the table: (a) close the item as a doc error with no code change, since the premise didn't hold, or (b) build the missing catalog data from the 2024 PHB's own d100 Trinkets table (verifiable against this project's rules-authority) plus the wizard UI to surface it — the larger, first-ever `packages/web` change of this whole cycle. **User chose (b)**.
- Rule source: `docs/players-handbook-2024/Chapter 2- Creating a Character/chapter2- creatingCharacter.md` lines 771-874, the "### Trinkets" d100 table (100 entries, verbatim) — line 23's own "you can choose... one trinket" grant.
- Migration (`1784269841666_add-trinket-item-type.ts`): adds `'trinket'` to the `items_item_type_check` CHECK constraint (verified the exact existing constraint definition via `pg_get_constraintdef` before drop/recreate, per this project's standing migration discipline). `schemas/catalogHomebrew.ts`'s `itemHomebrewShape.itemType` enum extended to match, so DM homebrew item creation also accepts the new type.
- **Modeled as ordinary `items` catalog rows** (`item_type='trinket'`), not a new dedicated table — this reuses the entire existing `character_items` inventory pipeline with zero new join-table/plumbing work: a trinket becomes a real inventory item exactly like any other piece of starting gear.
- `db/seeds/catalog.ts`: new `seedTrinkets` inserts all 100 entries as `items` rows, `edition_scope='2024'` only (explicitly not backfilling a 2014 trinket table — this project's rules-authority under `docs/players-handbook-2024/` has no 2014 trinket text to verify against, same "only for editions this project can actually verify" precedent as P1-5's subclass catalog), `weight_lb=0`, `cost_cp=0`, slug `trinket-NNN` and name `Trinket #NNN` both zero-padded (`padStart(3,'0')`) so `services/catalog.ts`'s `listItems` `ORDER BY name ASC` sorts all 100 in correct roll order rather than lexicographically ("Trinket #1", "Trinket #10", "Trinket #100", "Trinket #11"...) — caught and fixed before the seed was finalized.
- `packages/web/src/lib/types.ts`'s `ItemType` union, `ItemCatalogBrowser.tsx`'s `ITEM_TYPES` filter list, and `catalogEntities.ts`'s homebrew item-type `options` all extended with `'trinket'`; `items.type.trinket` added to all three locale dictionaries (en/es/fr).
- `characters/wizard/EquipmentStep.tsx` (the actual UI): a new bordered section rendered above the generic equipment-adder, reusing the wizard's existing `draft.equipment: WizardEquipmentPick[]` array and its already-working deferred-POST submission path unchanged (per the file's own header comment, nothing is sent to the server until the wizard's Equipment→Portrait transition creates the character) — no changes needed to `types.ts`, `validation.ts`, or character-creation submission code. Offers "Roll a trinket" (random pick from the 100) or a `Combobox` search-and-pick by description text; once picked, shows the trinket's description with Reroll/Remove actions. New `characters.wizard.trinket.*` keys added to all three locale dictionaries (en/es/fr) — a French copy-paste slip (`reroll` accidentally duplicating `remove`'s "Retirer") was caught and corrected to "Tirer à nouveau" before verification.
- Tests: `services/catalog.trinketsCompleteness.integration.test.ts` (new, 4 cases, live DB) — exactly 100 rows for `edition_scope='2024'`; every trinket has a non-empty description and `weight_lb`/`cost_cp` both 0; first/last descriptions match the source table verbatim; `ORDER BY name ASC` yields correct roll order (spot-checked index 0, 9, and the last entry).
- Deliberately did NOT add a new `.test.tsx` component test for the wizard UI change — confirmed via search that this frontend codebase has zero existing component-level tests (only `.test.ts` for pure logic anywhere under `packages/web`), so the existing convention was followed rather than introducing a new pattern unilaterally.

**Verification:**
- `npx tsc -p tsconfig.json --noEmit` in `packages/server`: clean.
- `npx tsc -b --force` in `packages/web` (plain `tsc --noEmit` is a no-op in this package): clean.
- `npm run migrate` and `npm run seed` applied cleanly against the live dev DB (items count 20 → 120); spot-checked via direct SQL that `trinket-001`/`trinket-100` are correct and `ORDER BY name ASC` returns roll order 1-100.
- `npm test` in `packages/server` (live DB): 854/855 pass — the 1 failure is `campaignClasses.integration.test.ts`'s pre-existing test-data-collision flake, documented in nearly every prior phase's entry, unrelated to this change.
- `npm test` in `packages/web`: 148/148 pass, including the locale-completeness test confirming identical key sets across en/es/fr for the new `items.type.trinket` and `characters.wizard.trinket.*` keys.
- Both dev servers (`npm run dev:server`, `npm run dev:web`) were started and confirmed listening (`http://localhost:3001`, `http://localhost:5173`) with no compile or runtime errors.
- **Live browser UI verification was NOT completed** — `mcp__claude-in-chrome__tabs_context_mcp` failed both times it was attempted ("Browser extension is not connected... ensure the Claude browser extension is installed and running, and that you are logged into claude.ai with the same account as Claude Code"). Per this project's own CLAUDE.md instruction, this is disclosed explicitly rather than claiming the wizard flow was exercised in a browser: the Trinket section's actual rendering, the Roll/Combobox/Reroll/Remove interactions, and end-to-end submission into a created character's inventory have only been verified by code review, typecheck, and the (non-UI) test suite — not by using the feature.

**Not done / stopped for:**
- **No 2014-edition trinket table** — by design; this project's rules-authority has no 2014 trinket text to seed from or verify against.
- **Live browser verification of the wizard UI was not possible this session** — blocked on the Claude Chrome extension not connecting; someone with browser access should click through Character Creation → Equipment step once to confirm the Trinket section renders and round-trips correctly before treating this as fully closed.
- **No new component-level test** — matches this frontend codebase's existing zero-`.test.tsx` convention; if that convention changes project-wide, this is the first UI change that would need one retrofitted.

## Phase 4 — P3 optional (done)

**P3-1: Fall damage (ER-06).**
- Acceptance criteria: a creature that falls takes 1d6 Bludgeoning damage per 10 ft fallen (max 20d6) and lands Prone unless it avoids taking any damage from the fall.
- **This item was explicitly gated in the gap analysis itself**, on two fronts: Open Question 4 ("is exploration/travel mechanics scope ever intended for this project") and a real dependency gap ("blocked on elevation/pit-trigger support not existing in the map model"). The gap analysis's own "Not doing" section says not to implement any P3 item without resolving Open Question 4 first. **Confirmed with the user via AskUserQuestion before any code**: build the elevation/pit-trigger map-model foundation now, then wire fall damage on top of it — the other option ("track-state-only, no map model change") and "skip P3 entirely" were both declined.
- Rule source: `rulesGlossary.md` line 858-862, "Falling [Hazard]" — "1d6 Bludgeoning damage at the end of the fall for every 10 feet it fell, to a maximum of 20d6... the creature lands, it has the Prone condition unless it avoids taking any damage from the fall." Confirmed identical in 2014 (`.opencode/skills/dnd5e-srd/references/2014/adventuring.md`'s own "Falling" section, 1d6/3m same cap) — no edition branch needed, unlike most of this roadmap's P0/P1 items.
- **The map-model foundation, built as two small DM-tunable board attributes, not a physics simulation** (matching `vision_radius_ft`/Cover's own established "small DM knob" shape):
  - Migration `1784269842666_add-participant-elevation.ts` — `combat_participants.elevation_ft` (current height above the map's ground plane).
  - Migration `1784269843666_add-map-cell-pit.ts` — a new `map_cell_overrides.cost_type = 'pit'` value plus `pit_depth_ft`, reusing the exact same DM-authored per-cell table difficult/impassable/special terrain already use rather than a new table. `services/movement.ts`'s `computeStepCost` deliberately does NOT treat `'pit'` as extra movement cost (a hidden pit trap doesn't visibly cost more to step onto than a normal cell — that's the whole point of it being hidden); it falls through to the same normal-cost path as an unlisted cell.
  - `services/encounters.ts`'s `computeValidatedMoveCost` (feeding `setParticipantPosition`) now also reports `pitTriggered: { depthFt } | null` — a purely geometric fact (did this move END on a DM-placed pit cell), so unlike Cover/Surprise/Hide this is detected automatically, but — matching this whole roadmap's "compute-and-suggest, never auto-apply" precedent for `opportunityAttackTriggers` right beside it — only ever REPORTED, never auto-resolved. The caller resolves the actual fall via a separate, explicit `POST .../fall` call.
  - New DM-only `PATCH /encounters/:id/participants/:pid/elevation` (`setParticipantElevation`), same shape/resync as `PATCH .../cover`. `elevation_ft` is also now in `getEncounterCombatSnapshot`/`buildFullStateSyncPayload` (`elevationFt`), same "not HP-sensitive, no redaction" treatment as cover/vision.
- **The damage application itself deliberately reuses the EXISTING `applyDamage`/`applyMonsterInstanceDamage` pipeline** rather than a parallel HP-mutation path — fall damage is ordinary bludgeoning damage subject to the exact same resistance/vulnerability/immunity, temp-HP absorption, and death-save/massive-damage/unconscious transitions any other hit is. `services/damage.ts`'s new pure `computeFallDamageDiceCount(distanceFt)` (`min(20, floor(distanceFt/10))`) is the only new derived input those functions need — matches `applyDamageSchema`'s own `diceCount: 0` precedent (P1-6's Graze).
- **Found and sidestepped a real, pre-existing gap while wiring this up** (flagged, not fixed — fixing it touches the shared apply-damage endpoint, out of scope here): `dice_rolls` has a live DB `CHECK (dice_count >= 1)` constraint that a 0-count roll violates whenever `encounterId` is also supplied — reachable today via `applyDamageSchema`'s own documented `diceCount: 0` precedent (P1-6's Graze), which nothing before this phase ever actually exercised end-to-end (Graze itself only returns descriptive text telling the caller to make that call, it doesn't make it). A fall under 10 ft (`diceCount === 0`, guaranteed 0 damage) is the first real caller to hit this, so `services/fallDamage.ts`'s `performFallDamage` short-circuits before calling `applyDamage`/`applyMonsterInstanceDamage` at all in that case — also the mechanically correct thing to do (there's nothing to roll), not just a workaround.
- `services/fallDamage.ts` (new) — `performFallDamage(pool, encounterId, participantId, actorId, input)`: resolves the participant's character-vs-monster-instance dispatch, computes dice count from `distanceFt`, calls the existing damage pipeline (bludgeoning, `isCritical` always false — a fall never has a backing attack roll — and no `attackerParticipantId`, so neither function's DM-approval pending-queue branch is ever reached; a fall isn't an attack someone else is making, it's the same "acting on your own target" gate `applyHpDelta`/`spendHitDice` already use), applies the real seeded "Prone" condition-linked `effect_definitions` row via `insertActiveEffect` when (and only when) `appliedDamage > 0`, and updates `elevation_ft` (clamped at 0) in every case.
- The optional "fall into water" reaction check (`rulesGlossary.md` line 862 — DC 15 Str (Athletics) or Dex (Acrobatics) to halve damage) reuses P1-12's exact `saveDc`/`savingThrowRollId`/`halfOnSave` fields and `deriveSaveOutcomeSucceeded` re-derivation — no new save-handling code, same "never trust a client-asserted succeeded/failed boolean" invariant.
- Route: `POST /encounters/:id/participants/:pid/fall`, `requireOwnParticipantOrDm`-gated like Hide/Shove/Grapple (a player may resolve their OWN character falling, the DM anyone's). Not an action-economy spend (falling isn't a chosen action, matching `spend-hit-dice`'s own precedent) — no `action_used`/`recordAction` touched, just HP/Prone/elevation. Broadcasts `HP_CHANGED`/`CONCENTRATION_CHECK_PROMPTED`/`EFFECT_APPLIED` same as the underlying damage/effect calls, plus a `broadcastFullStateResync` at the end since elevation isn't part of any of those existing event shapes (same "small board attribute, resync the snapshot" precedent as Cover).
- Tests: `services/damage.test.ts` gains a `computeFallDamageDiceCount` describe block (6 pure cases — under-10ft floor-to-zero, one-die-per-10ft, the 20d6 cap, negative-distance guard). `services/movement.test.ts` gains a `pit` cost-type regression case (normal movement cost, not difficult/impassable). `services/fallDamage.integration.test.ts` (new, 8 cases, live DB, throwaway fixtures) — a 35 ft fall's dice count/HP reduction/Prone; a sub-10ft fall's zero dice/zero damage/no Prone; elevation tracking clamped at 0; bludgeoning resistance halving; a successful reaction save halving; monster-instance parity (dispatch + Prone); the 20d6 cap. `services/encounters.pit.integration.test.ts` (new, 2 cases, live DB, single-row corridor map — same "no cheaper diagonal detour" trick `encounters.incapacitatedOccupancy.integration.test.ts` uses) — moving onto a pit cell costs normal movement AND reports `pitTriggered` with its depth; a move that doesn't land on the pit cell reports `pitTriggered: null`.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean.
- `npx tsc -b --force` in `packages/web`: clean (no web changes this phase; checked since `packages/web` shares no types with this phase's work, only to confirm no accidental cross-package breakage).
- `npm run migrate` applied cleanly against the live dev DB; `npm run seed` re-run cleanly.
- `npm test` in `packages/server` (live DB, freshly reseeded): 869/869 pass.

**Not done / stopped for:**
- **P3-3 (Environmental hazards)** — done in a later pass (see its own entry below), on the same narrow-scope basis (a stateless calculator + advisory endpoints, not a blanket "P3 is in scope" decision).
- **No frontend wiring** — `packages/web` has no UI yet for a DM to place a pit cell, set a participant's elevation, or trigger the Fall action/see the pitTriggered prompt after a move. Server-only, same scope boundary as every other phase this cycle.
- **Fall detection stays entirely manual/reported, never auto-resolved** — `pitTriggered` is computed and returned from a move, but nothing calls `POST .../fall` automatically; a DM/player must make that second call themselves, matching Cover/Surprise/Hide's "DM adjudicates, server suggests" precedent (this pit case is closer to "server suggests" than the others, since detection itself IS automatic — see the `computeValidatedMoveCost` comment above — but resolution deliberately isn't).
- **The `dice_rolls_dice_count_check` gap this phase found is unfixed** — `POST /characters/:id/apply-damage`/`POST /monster-instances/:id/apply-damage` still can't actually accept `diceCount: 0` alongside `encounterId` (violates the live DB constraint), despite `applyDamageSchema`'s own comment documenting that combination as intentional (P1-6's Graze). Flagged as a real, separate follow-up rather than fixed here — this phase's own fall-damage caller only avoids it by never making that call for a 0-dice fall.
- **No "fall into water" auto-detection** — the optional reaction-save halving reuses P1-12's save fields, but nothing determines whether the landing cell is actually water; the DM/player supplies `savingThrowRollId`/`saveDc` only when they've decided (via `map_cell_overrides.medium = 'water'` or their own judgment) that it applies.
- **Rage's fall-damage interaction, and other temporary-resistance sources, are correctly covered for free** (this reuses `applyDamage`'s existing `fetchActiveGrantedResistances` union) — not a gap, noted only because it wasn't separately tested end-to-end for the fall path beyond the one `damage.test.ts`-style resistance case already covering `computeAppliedDamage` generically.

**P3-2: Travel pace (ER-07).**
- Acceptance criteria (roadmap text): Fast/Normal/Slow travel speed + the skill-check (dis)advantage each pace imposes, currently absent from the server entirely (`grep travelPace packages/server/src` = 0 hits).
- **This item was gated in the gap analysis** (Open Question 4, and P3-1's own note that Open Question 4 was "resolved narrowly … not a blanket 'P3 is in scope' decision"). **Scope confirmed with the user via AskUserQuestion before any code**: a **stateless calculator** (pure module + one read-only endpoint), over the two heavier options (a persisted `travel_journeys` table, or that plus automatic forced-march exhaustion application). **Marching order** — also covered by the same reference section — was explicitly excluded as a separate follow-up.
- Consulted the `dnd-rules` agent first (per this project's convention), which produced a source-cited edition-by-edition ruling and wrote `docs/rules/travel-pace.md` (full citation chain into `.opencode/skills/dnd5e-srd/references/2014|2024/adventuring.md` and `docs/players-handbook-2024/Chapter 1- Playing the Game/`). Key findings that shaped the implementation:
  - **Pace table is identical in both editions** (Fast 400 ft/min · 4 mi/hr · 30 mi/day; Normal 300 · 3 · 24; Slow 200 · 2 · 18) — so `TRAVEL_PACE_TABLE` is a single non-edition-keyed constant. The table is internally inconsistent between its own columns (400 ft/min ≠ 4 mi/hr); the module reports raw figures and derives hour-scale distance from `milesPerHour` (what a DM plans around), documented in a comment.
  - **Pace effects are a hard 2014/2024 divergence**, edition-branched in `paceEffects()`: 2014 Fast = flat −5 to *passive* Perception only (not Disadvantage, not active checks/Survival/Stealth); 2014 Normal = nothing; 2014 Slow = a stealth/search *permission* with no die bonus. 2024 Fast = Disadvantage on Wis (Perception **and** Survival) **and** Dex (Stealth); 2024 Normal = Disadvantage on Dex (Stealth) only; 2024 Slow = Advantage on Wis (Perception/Survival). A waterborne vessel applies no pace effects in either edition.
  - **Difficult terrain also diverges**: 2014 halves distance (×0.5, single application — the text offers no stacking mechanism); 2024's travel rules defer terrain to the DMG (not in this repo), so the RAW-faithful default is to leave 2024 distance **unmodified** and say so in a note rather than silently borrowing the 2014 or the combat-movement rule.
  - **Forced March is 2014-only** — 2024 removed the mechanic entirely (verified across PHB 2024 Ch1 and SRD 5.2). `forcedMarchSchedule()` returns, for a 2014 campaign, the advisory schedule of Con saves a DM would call for past the daily limit (DC = 10 + hours-past-limit; only *full* hours past the limit trigger a save; land-vehicle passengers flagged as ambiguous; waterborne exempt at up to 24 h/day). For 2024 it always returns `applies: false` with an explanatory note. **Nothing rolls a d20, touches `dice_rolls`, or writes `characters.exhaustion_level`** — exhaustion stays a manual DM action through the existing `/characters/:id/exhaustion` endpoint, matching the "compute-and-suggest, never enforce" precedent of P2-2/P2-6.
  - **Mount "double distance for ~1 hour" burst** also diverges in wording: 2014 = twice the *Fast*-pace distance regardless of the party's chosen pace (recovery period unspecified in 2014); 2024 = twice the *chosen* pace's distance, then the mounts need a Short or Long Rest. `mountedBurst()` branches on this.
  - **The "8 hours/day" baseline is not hardcoded** — `hoursPerDay` is an optional param (default `DEFAULT_TRAVEL_HOURS_PER_DAY = 8`) feeding both the forced-march threshold/DC and the per-day distance figure.
  - **Speed-scaling deliberately not modeled** — the SRD gives the table only for Speed 30 and no scaling formula in either edition (`[not found in source]`); the plan carries a note that figures aren't adjusted for slower/faster travelers and that a group moves at its slowest member's rate.
- Files: `domain/travelPace.ts` (new, pure, no DB — same `domain/` placement and pure-function shape as `domain/obscurement.ts`), `schemas/travelPace.ts` (new — a coerced query schema with a `.refine` requiring `vesselSpeedMilesPerHour` when `mode === 'waterborne'`, mirroring `performFallSchema`'s conditional-field pattern), `services/travelPace.ts` (new, thin — resolves the campaign's `srd_edition` and hands it to the pure `computeTravelPlan`, 404s an unknown campaign; nothing written), `routes/travelPace.ts` (new — `GET /campaigns/:id/travel-pace`, `requireAuth` + `requireCampaignMember`, any member may call it since it exposes no hidden data), wired into `index.ts` beside the locations router.
- Tests: `domain/travelPace.test.ts` (new, 25 pure cases) — the table constants, distance math (fractional hours, never reading the Day column for a partial day), both editions' difficult-terrain behavior, every pace's effect in both editions (incl. the 2024-Survival-is-included and 2024-Slow-doesn't-touch-Stealth specifics), the forced-march DC formula + partial-hour + non-default-`hoursPerDay` + land-vehicle + waterborne branches, 2024-never-forced-march, and the 2014-vs-2024 mount-burst basis. `services/travelPace.integration.test.ts` (new, 3 cases, live DB, throwaway campaign fixtures) — the SAME query returns a −5-passive-Perception plan for a real 2014 campaign and a Disadvantage-list plan for a real 2024 campaign (identical distance), forced march schedules saves for 2014 / never for 2024, and an unknown campaign 404s.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean. `npx tsc -b --force` in `packages/web`: clean (no web changes; checked for accidental cross-package breakage only).
- `npm test` in `packages/server` (live DB): 897/897 pass (869 prior + 25 pure + 3 integration). The pre-existing `campaignClasses.integration.test.ts` collision noted since Phase 1 is no longer failing on this run.
- No migration — this phase adds no schema, no table, no column, and persists nothing.

**Not done / stopped for — each a real, separate follow-up:**
- **No persistence / journey tracking** — by the user's explicit choice. The calculator is stateless: there is no travel history, no "hours traveled today" accumulator, no cross-day forced-march DC continuity. A DM who wants a campaign travel log needs the `travel_journeys`/`travel_journey_legs` tables the `dnd-rules` agent's `docs/rules/travel-pace.md` §2 sketches.
- **Forced march never applies exhaustion** — the schedule of Con-save DCs is reported; the DM rolls them and applies any exhaustion manually via `/characters/:id/exhaustion`. No `dice_rolls` row, no server-side save re-derivation (unlike P1-12/P3-1's `deriveSaveOutcomeSucceeded`), no automatic `characters.exhaustion_level` write, no death-at-6 handling.
- **Marching order** (traps / spotting / combat-start positioning — the other half of the same reference section) — excluded with the user; a separate follow-up that could tie into P3-1's pit cells.
- **Speed ≠ 30, mixed-Speed parties, per-character pace** — not modeled (`[not found in source]` for any scaling formula); the plan notes the flat-table assumption.
- **2024 difficult-terrain and DMG pace-restriction rules** — the 2024 branch records terrain as advisory only (no distance change); a DM opt-in to the 2014-style halving, or to "difficult terrain forbids the faster paces," is not offered (no campaign config columns this stateless pass).
- **Vehicle/mount catalog speeds** — `vesselSpeedMilesPerHour` is a caller-supplied number; there is no `vehicles`/`mounts` catalog with a Speed field, and special/flying mounts have no overland rule to model.
- **No frontend wiring** — `packages/web` has no UI calling `GET /campaigns/:id/travel-pace`. Server-only, same scope boundary as every phase this cycle.

**P3-3: Environmental hazards — Burning, Dehydration, Malnutrition, Suffocation (ER-08).**
- Acceptance criteria (roadmap text): the four non-falling hazards, "Zero matches for these terms anywhere in `packages/server/src`" — resolve to Fire damage (Burning) or Exhaustion (the other three).
- **This item was gated in the gap analysis** (Open Question 4, and both P3-1 and P3-2's own notes that OQ4 stays "resolved narrowly … not a blanket 'P3 is in scope' decision for P3-3"). **Scope confirmed with the user via AskUserQuestion before any code:** a **stateless calculator** (pure `domain/hazards.ts` + thin service + advisory endpoints, same shape as P3-2), over the heavier options (a persisted per-day food/water log, or skipping P3-3). Second decision, also confirmed: the end-of-day / suffocation resolutions **auto-write** the computed Exhaustion delta to `characters.exhaustion_level` in the same transaction (like P2-4 / `fallDamage`), rather than P3-2's forced-march "report only".
- Rules research done directly against primary sources (2024: `docs/players-handbook-2024/Rules Glossary/rulesGlossary.md`; 2014: `.opencode/skills/dnd5e-srd/references/2014/adventuring.md` + `conditions.md`), plus the `dnd-2024-rules` skill for the consolidated 2024 ruling. Full writeup with per-line citations: `docs/rules/environmental-hazards.md`. Key findings:
  - **Every one of the four hazards diverges between 2014 and 2024** — so every function in `domain/hazards.ts` is edition-branched (P0-1 / P1-9 / P3-2 precedent). Suffocation is the widest gap: **2024 = 1 Exhaustion level at the end of each turn, all removed on breathing again; 2014 = survive `max(1, CON mod)` rounds, then drop to 0 HP and dying, no Exhaustion at all.** Dehydration: 2024 has no save (`< half` → 1 level); 2014 is a DC 15 Con save when at least half was drunk, automatic below half, **and doubles to 2 levels if the creature was already exhausted** (`adventuring.md:147`). Malnutrition: 2024 is a DC 10 Con save for partial eating + a separate 5-day auto-escalation for total starvation; 2014 is a `3 + CON mod` grace period then auto levels. Burning: 1d4 Fire/turn in both, but 2024 is a codified Hazard with a Prone-and-roll extinguish action while **SRD 5.1 has no generic Burning hazard entry at all** (carried over as the common pattern, flagged, not re-verified — matching this project's "2014 isn't independently re-verified" convention).
  - **The 2024 Malnutrition asymmetry is encoded verbatim, not "fixed":** a strict reading of the glossary's two separate clauses punishes *partial* eating from day 1 but *total* starvation only from day 5. `malnutritionOutcome` follows the text; the oddity is flagged in-code and in `docs/rules/environmental-hazards.md` §3.
  - **Exhaustion caps at 6 = death in both editions.** `applyExhaustionDelta` clamps every write to `[0, 6]` and reports `reachedLethalLevel`, but the service **does not flip `characters.is_alive`** — matching the existing manual `updateExhaustion` endpoint, which also stops at the column write. Flagged below.
- **No migration.** Reuses `characters.exhaustion_level`, the `applyDamage` / `applyMonsterInstanceDamage` pipeline (Burning is ordinary Fire damage — Resistance/Vulnerability/Immunity, temp-HP, death-save/massive-damage transitions all apply for free), and `active_effects.stack_count` (the same mechanism P2-6 uses for a monster's Exhaustion level). Two seed-only `effect_definitions` templates added to `SPELL_EFFECT_DEFINITIONS` (`db/seeds/catalog.ts`): `Burning` (`refresh`) and `Suffocating` (`stack`, so its `stack_count` is a meaningful accrual ledger). `npm run seed`: `effect_definitions` 24 → 26.
- `domain/hazards.ts` (new, pure, no DB — same `domain/` placement and shape as `domain/travelPace.ts` / `domain/obscurement.ts`): `applyExhaustionDelta`, `burningTick`, `dehydrationOutcome`, `malnutritionOutcome`, `suffocationOutcome`, and the `WATER_NEEDS_GALLONS_PER_DAY` / `FOOD_NEEDS_POUNDS_PER_DAY` (2024) size tables. Every rule branch lives here; the service only resolves `srd_edition` and hands it in.
- `services/hazards.ts` (new):
  - `performBurningTick(pool, encounterId, participantId, actorId)` — near-clone of `services/fallDamage.ts` (fixed 1d4, no distance, no save). Requires an active `Burning` effect (the DM applies/removes it via the normal effects endpoints — "extinguished" = removed), then applies 1d4 Fire via the existing pipeline. `POST /encounters/:id/participants/:pid/burning-tick`, `requireOwnParticipantOrDm`.
  - `resolveDailyHazards(pool, actorId, campaignId, input)` — DM-only (`requireDm(await requireMembership(...))` before the transaction, the P2-5 pattern). One transaction, `FOR UPDATE` per character. For each entry computes Dehydration + Malnutrition outcomes, re-derives the DC 15 / DC 10 Con saves from a stored `dice_rolls` row via `deriveSaveOutcomeSucceeded` (P1-12's "never trust a client boolean" invariant — a missing/foreign roll id counts as a failed save), sums the levels, writes `exhaustion_level = clamp(0, 6, current + gained)`. `POST /campaigns/:id/hazards/resolve-daily` (new `routes/hazards.ts` → `campaignHazardsRouter`, mounted beside `travelPaceRouter` in `index.ts`).
  - `performSuffocationTick(pool, encounterId, participantId, actorId, input)` — **character participants only** (a monster instance has no `exhaustion_level` column — rejected with a message pointing at the manual-`Exhaustion`-effect workaround). 2024: each `canBreatheAgain: false` tick writes `+1` Exhaustion and increments the `Suffocating` ledger effect's `stack_count` (plain direct INSERT/UPDATE in the transaction, not `insertActiveEffect`'s spell machinery); `canBreatheAgain: true` subtracts the whole `stack_count` and soft-removes the effect — honouring "removes all Exhaustion it gained from suffocating" without a per-source model. 2014: report-only, writes nothing (the 0-HP/dying transition is a death-machine interaction, out of scope). `POST /encounters/:id/participants/:pid/suffocation-tick`, `requireOwnParticipantOrDm`.
- `schemas/hazards.ts` (new): `burningTickSchema` (`.strict()` empty body), `suffocationTickSchema` (`{ canBreatheAgain: boolean }`), `resolveDailyHazardsSchema` (`entries[]` of `{ characterId, size?, water?: { gallonsConsumed, hotWeather?, saveRollId? }, food?: { poundsConsumed, consecutiveDaysWithoutFood?, saveRollId? } }`, each entry `.refine`d to require at least one of water/food). Characters have no `size` column (they default to Medium), so `size` is a per-entry DM override; the server reads the character's own `con` for the 2014 grace period and its own `exhaustion_level` for the 2014 doubling rule.
- Tests: `domain/hazards.test.ts` (new, 20 pure cases) — the size tables, `applyExhaustionDelta` clamp/lethal, Burning both editions, every Dehydration branch (2024 no-save, 2014 DC 15 / auto / already-exhausted doubling / hot weather), every Malnutrition branch (2024 DC 10 save, 2024 5-day starvation escalation and the day-1-4 no-op, 2014 `3 + CON mod` grace with the min-1 floor, 2014-never-a-save), Suffocation breath-hold formula + 30s floor + the 2014/2024 post-breath divergence. `services/hazards.integration.test.ts` (new, 10 cases, live DB, throwaway fixtures) — 2024 dehydration writes `exhaustion_level`, 2024 malnutrition DC 10 save **re-derived from a real `dice_rolls` row** (pass vs. fail), 2014 already-exhausted doubling, the clamp-at-6 write, a well-fed no-op; Burning rejects without an active effect then applies 1d4 Fire, Burning respects Fire Resistance via the shared pipeline; Suffocation 2024 accrues +1/tick and breathe-again removes exactly the suffocation levels (leaving a pre-existing level intact) and clears the ledger effect, 2014 suffocation writes nothing, a monster-instance participant is rejected.

**Verification:**
- `npx tsc -b --force` in `packages/server`: clean. `npx tsc -b --force` in `packages/web`: clean (no web changes; checked for accidental cross-package breakage only).
- `npm run seed` re-run cleanly against the live dev DB (`effect_definitions` 24 → 26 — `Burning`, `Suffocating`).
- `npm test` in `packages/server` (live DB): 927/927 pass (897 prior + 20 pure + 10 integration). The pre-existing `campaignClasses.integration.test.ts` collision noted since Phase 1 did not reproduce this run.
- No migration — this phase adds no schema, no table, no column, and persists nothing beyond the `exhaustion_level` writes and the `Suffocating` ledger effect row.

**Not done / stopped for — each a real, separate follow-up:**
- **Exhaustion 6 ⇒ `characters.is_alive = false`** — `applyExhaustionDelta` clamps at 6 and reports `reachedLethalLevel`, but nothing wires the death transition into the P1-1 death state machine. Matches the existing manual `updateExhaustion` endpoint, which also never does; wiring it is a shared concern for both, not something this phase does silently.
- **The "Exhaustion from dehydration/malnutrition can't be removed until you eat/drink a full day" precondition** — `characters.exhaustion_level` is a single integer with no per-source tracking, so P2-4's Long Rest auto-reduction can't distinguish a hazard level from any other. Would need a per-source Exhaustion model (schema change).
- **2014 Suffocation's 0-HP / dying transition** — report-only; auto-applying it reaches into the death state machine (out of scope for this "compute-and-suggest" pass), same treatment as P3-2's forced-march exhaustion.
- **Monster suffocation via `suffocation-tick`** — character participants only (no `exhaustion_level` column on `monster_instances`); the DM tracks a suffocating monster via a manual `Exhaustion` effect's `stack_count`, same as every other monster-Exhaustion case (P2-6).
- **No in-game clock / per-day food-and-water persistence** — the DM supplies the day's consumption and the running "consecutive days without food" tally on each `resolve-daily` call. Same "no clock" wall as P2-5 (rest-hour timing) and P3-2 (forced-march timing). No `hazard_days` table, no cross-day continuity.
- **`elapsedMinutes`-style trust** — the DM-supplied `consecutiveDaysWithoutFood`, `size`, and `hotWeather` are trusted as-is (nothing to verify them against), same as P2-5's `elapsedMinutes` and P3-2's terrain/mode inputs.
- **The 2024 Malnutrition day-1-to-4 total-starvation no-op** is a strict reading of two separate glossary clauses, not a bug — encoded verbatim and flagged in `docs/rules/environmental-hazards.md` §3 so a future reader doesn't "fix" it into a silent divergence from RAW.
- **No frontend wiring** — `packages/web` has no UI for a DM to apply Burning / run `resolve-daily` / tick suffocation. Server-only, same scope boundary as every phase this cycle.
