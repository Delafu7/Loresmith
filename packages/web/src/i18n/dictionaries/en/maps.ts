// The two cross-campaign map pages (maps/MapsIndexPage.tsx and
// maps/FullscreenMapPage.tsx). Both reuse existing keys (nav.maps, nav.home,
// dashboard.noCampaigns, encounters.status.*) for everything except this one
// bit of standalone text.
export const maps = {
  backToMaps: '← Maps',
};
